# Research Landscape

> This document tracks the evolving state of the field as researcher reads papers.
> Researcher maintains this file; human reviews changes via PR diff.

## Overview

Decision agent 技术路径综述。关注决策架构的设计模式、技能管理机制，以及多智能体协同完成长期任务的方法。

## §1 Decision Agent Architecture

### §1.1 Co-Evolving Multi-Agent Systems

- **COS-PLAY** [1] (Wu et al., 2026) — 将决策智能体（A_D）和技能库智能体（A_S）解耦并用 GRPO 联合训练，通过 7–25 轮协同进化达到收敛。8B 模型（Qwen3-8B）在四款单人游戏上平均奖励超 GPT-5.4 25.1%；Diplomacy 多人游戏优于 Gemini-3.1-Pro 8.8% [1: §5.1]。关键约束：技能库分布与决策策略分布必须对齐——错位版本反而降低性能 [1: §5.2]。技能可复用性：每技能平均 12–49 次实例化，2–6 次合约精化 [1: §5.3]。
  - *builds-on* Voyager (Wang et al., 2023) — COS-PLAY 将 Voyager 的外部技能库扩展为一个独立训练的技能库智能体 [med]
  - *competes-with* SkillRL (Xia et al., 2026), SCALAR (Zabounidis et al., 2026) — 两者同样将技能学习与 RL 结合，但技能库均为静态基础设施而非独立训练的协作智能体 [med]
  - *relation to thesis* — 直接实例化"multi-agent workers team"架构；技能库智能体跨 episode 无状态运行（无持续上下文），为决策智能体独立部署提供初步支持证据 [1: §Relations]

> *Emerging sub-cluster: Graph-based Test-time Routing* — 在推理时通过有向图结构进行智能体选择与消息传递，无需 RL 训练。GoA 不适配 §1.1（Co-Evolving），放置于父桶 §1；建议增设 §1.2，详见 `contradictions.md §Proposed taxonomy extension`。

- **GoA** [2] (Yun et al., 2026, ICLR 2026) — 将多智能体协作建模为有向图，分四阶段执行：①元 LLM 从模型卡采样 top-k 相关智能体（节点采样）；②各智能体互相评分生成有向相关权重边（边采样）；③双向消息传递（高相关节点→低相关节点，再反向）；④图池化输出（GoAMax 取最高连接节点响应，GoAMean 由元 LLM 加权合成）。GoAMax（3 agents）在 MMLU 79.18、MMLU-Pro 54.78、MedMCQA 60.04 上超越 MoA（6 agents）；GoAMean 在 GPQA 40.54、MATH 73.12 上超越 MoA；推理成本降低约 58%（11 vs. 19 LLM calls，19k vs. 56k tokens）[2: §4.2, Table 2]。关键机制：相关性有向消息传递——反转流向造成最大性能下降（–2.60 MMLU-Pro，–5.05 GPQA）[2: §4.4, Table 5]。
  - *competes-with* [1] (COS-PLAY) [low]: GoA 为零样本推理时框架，COS-PLAY 为带 RL 训练的长时序任务架构；均采用多智能体协同，但操作层级完全不同（QA 推理路由 vs. 游戏任务联合进化）
  - *competes-with* MoA (Wang et al., 2024) [high]: GoA 证明 MoA 是其退化特例（k=N、全连接均匀权重、mean pooling），并在所有主要基准上系统性超越 MoA [2: §3.3, §4.2]
  - *relation to thesis* — GoA 处理推理层智能体路由（单轮多域 QA），非决策智能体的多步任务执行；图式节点采样（元 LLM 按查询动态路由至专家子智能体）可映射为 openclaw→decision agent 调度链的轻量实现，与论题互补但不直接支持或反驳核心部署独立性主张 [2: §Relations]

> *Emerging sub-cluster: Single-Agent Future-Aware Planning* — 从理论上证明步进式推理（CoT/ReAct/Reflexion）等价于贪心策略，无法在长时序任务中替代前瞻规划。FLARE 不适配 §1.1（Co-Evolving）或图路由子簇，放置于父桶 §1；建议增设 §1.3，详见 `contradictions.md §Proposed taxonomy extension`。

- **FLARE** [3] (Wang et al., 2026) — 形式化证明步进式 LLM 推理（CoT、ReAct、Reflexion）等价于最大化局部代理分数的贪心策略，从结构上不足以支撑长时序规划 [3: §2, §3.2]。关键实证：第一步决策中，贪心策略在 55.6%、beam search 在 71.9% 的情况下选中局部最优但全局次优的"陷阱"动作，前瞻仅 23.6%；首步错误后单步恢复概率 5.4%（贪心）vs. 29.7%（FLARE）[3: §3.1, Table 3]。FLARE 通过 MCTS + 后向价值传播 + 轨迹记忆实现前瞻规划：CWQ Hits@1 71.8（FLARE）vs. 59.8（单步）[3: §5.2, Table 1]；LLaMA-8B + FLARE 频繁超越 GPT-4o + 标准推理，表明规划能力无法通过参数规模恢复 [3: §5.2]。关键局限：假设确定性转移、显式状态与规划时可用评估信号；单智能体评测，无多智能体协同证据 [3: Limitations]。
  - *orthogonal to* [1] (COS-PLAY) [med]: COS-PLAY 改进智能体"知道做什么"（技能库），FLARE 改进"如何在已知动作中选择"（规划机制）；两者解决正交缺口，组合系统需要二者 [1: §6, 3: §7]
  - *orthogonal to* [2] (GoA) [low]: GoA 处理智能体间通信路由（单轮 QA），FLARE 处理智能体内多步动作选择；系统层级完全不同，无竞争关系
  - *relation to thesis* — 为论题提供关键约束：若 decision agent workers 采用步进式推理（当前主流），在需要多步序列决策的任务上系统性失败；支持论题扩展——workers 内部规划机制必须具备前瞻性，而非贪心局部。不挑战多智能体框架，但对 openclaw×decision agent 协作栈提出单智能体内规划先决条件 [3: §Relations]

> *Emerging sub-cluster: Single-Agent Sufficiency Baseline* — 形式化证明并实证验证同质多智能体工作流可折叠为单智能体顺序执行而无精度损失，成本更低。[4] 不适配 §1.1（Co-Evolving）、图路由子簇或前瞻规划子簇，放置于父桶 §1；建议增设 §1.4，详见 `contradictions.md §Proposed taxonomy extension`。

- **OneFlow** [4] (Xu et al., 2026) — Proposition 1 形式化证明：在确定性工具副作用、基于可见历史的路由策略、共享随机性三个前提下，单 LLM 模拟器与多智能体系统的轨迹分布完全相同 [4: §3.1]。OneFlow（单智能体执行 + MCTS 双元 LLM 工作流搜索）在 6 个公开基准上匹配或超越 AFlow（多智能体），成本大幅降低：HumanEval $0.020 vs. $0.198（约 10×），HotpotQA F1 73.5 vs. 72.1，成本 $0.278 vs. $1.438（约 5×）[4: §4.2.1, Table 1–2]。单智能体 KV cache 复用将前缀成本从 Σ L_t 降至 Σ ΔL_t（渐进节省）[4: §3.1]。关键约束：等价结论仅适用于同质工作流（所有 agents 共享同一基础 LLM）；异质工作流（混合模型）性能上界由最强同质工作流决定，未超越 [4: §2, §4.2.3]；所有实验 temperature=0，形式化等价不覆盖随机采样 [4: §4.1]；最复杂基准为 TravelPlanner（单会话规划），多会话企业工作流未覆盖 [4: §4.1]。**等价边界注记（基于 [9] 反证）**：折叠等价隐含"任务 fits 单 LLM 上下文"前提；当数据湖文件总量远超上下文窗口时，DS-GRU 风格的"全部塞 prompt"折叠在 Qwen3 / Claude 4 Opus 上分别只有 1.21% / 4.11%（见 [9: §4.2 Table 1]），等价构造物理上无法承载——OneFlow 适用于上下文充足任务，上下文必然溢出场景需多 agent 范式。
  - *contradicts* thesis §1 (multi-agent necessity) [med]: 论题以多智能体 workers team 为架构核心；[4] 证明同质配置下多智能体结构无精度收益且增加成本——部分证伪；Decision Agent 的 BKN+ContextLoader+Dolphin+ISF 专化组件若构成异质配置则结论不适用 [4: §3.1; thesis §1]
  - *competes-with* [2] (GoA) [med]: GoA 将图结构多智能体路由作为性能路径；[4] 证明同质设置下复杂路由结构不必要——GoA 使用异质 7–8B 专家，[4] 结论不直接反驳，但将"单智能体基线"作为效率门槛施加于所有多智能体路由设计 [4: §3.1; 2: §3.3]
  - *extends* [3] (FLARE) [low]: FLARE 证明步进式推理规划不足；[4] 进一步表明多智能体任务分解工作流可折叠为单智能体顺序推理而不损精度——合并暗示性能天花板由规划机制决定而非通信拓扑，但此为合成推断，两篇论文均未明确 [3: §7; 4: §6]
  - *orthogonal to* [1] (COS-PLAY) [med]: COS-PLAY 使用独立训练 LoRA 适配器的异质双智能体，属 [4] 自身标准下必须保留多智能体结构的场景；[4] 明确指出单 LLM 模拟"无法捕获异质工作流" [4: §6; 1: §3]

> *Emerging sub-cluster: Catalog-Layer Tool/Agent Retrieval* — 在工具/智能体目录上构建类型化知识图，用类型加权 RRF 进行召回；与 §1.2（GoA 推理时图路由协同输出）层级不同——此处图是静态目录结构，用于 pre-call 选择哪些 worker 被激活。[5] 不适配既有子簇，放置于父桶 §1；建议增设 §1.5，详见 `contradictions.md §Proposed taxonomy extension`。

- **Agent-as-a-Graph** [5] (Nizar et al., 2025) — 将 MCP 服务器（agents A）和工具（T）建模为二部图 G=(A,T,E)，边记录 owner(t)=a；检索时对 A 与 T 在统一向量空间独立 top-N，再通过类型加权 RRF（αT/(k+r) 用于工具节点，αA/(k+r) 用于 agent 节点，k=60）重排，最后沿 owner 边遍历至 K 个唯一 agent [5: §3.1, §3.2, Algorithm 1]。LiveMCPBench（70 服务器/527 工具/95 问题）：Recall@5 = 0.85、nDCG@5 = 0.47，相对 SOTA ScaleMCP（0.74/0.40）+14.9%/+14.6%；最优类型权重 αA:αT = 1.5:1（轻度偏向 agent 覆盖），1:1 退化至 0.83/0.46，3:1 退化至 0.76 [5: §4.2, §4.4, Table 1, Table 3]。跨 8 种 embedding 模型（Vertex/Gemini/Titan v1-v2/OpenAI ada-3小-3大/MiniLM）平均 Recall@5 提升 19.4%（std≈0.02），Titan v2 上+28.8% [5: §4.3, Table 2]。检索来源分布：39.13% 直接来自 agent 节点，34.44% 来自工具节点经 owner 边遍历——两类型节点都对最终路由作出实质贡献 [5: §4.2.1]。
  - *builds-on* ScaleMCP / Tool-to-Agent Retrieval (Lumer et al. 2025c-d) [high]: 同组前作；[5] 在 ScaleMCP "拼接描述单向量"基线上引入类型化节点+类型加权 RRF，将多跳查询失败模式转为"显式类型节点+图遍历"修复 [5: §1, §2.1]
  - *builds-on* RRF (Cormack et al. 2009) [high]: 标准 wRRF 是出发点，[5] 改为分段式 type-conditioned 形式（Eq. 3），保留共享分母 k+r(e) 以维持类型内 RRF 排序性质 [5: §3.3]
  - *competes-with* [2] (GoA) [med]: 均为多智能体架构中的"图"，但层级不同——GoA 在推理时构图协调多 agent 输出（消息传递+图池化），[5] 在调用前对静态目录构图选 agent；二者可组合（[5] 选 K 个 agent → GoA 协同输出），双方均未引用对方（concurrent）
  - *competes-with* [4] (OneFlow) [med]: [4] 质疑同质多智能体的必要性；[5] 在多智能体框架内默认其价值并优化路由层——若 [4] 同质等价结论成立，[5] 的检索方案失去前提；若异质性成立，[5] 的类型化召回直接服务于异质 worker 选择
  - *competes-with* [1] (COS-PLAY) [low]: 二者占据同一"系统如何知道调用什么能力"的位置——COS-PLAY 通过协同进化"学到"答案，[5] 在静态目录上"检索"答案；研究问题正交（技能习得 vs. 工具选择），但架构角色重合
  - *orthogonal to* [3] (FLARE) [high]: 规划与检索是不同问题；[5] 假设查询/子步已给定，仅负责选 agent，[3] 关注子步如何正确生成；可在同一栈共存（planner→[5]→worker）
  - *relation to thesis* — 直接命中 ContextLoader/工具管理研究缺口。论题明确将"工具检索精确召回（向量 vs 语义结构化对比）"列为高优先级；[5] 的"类型化节点显式分离 vs. 拼接单向量"恰好是该对比，14.9% Recall@5 提升构成 BKN"逻辑/行动分离"语义解耦的实证支撑。最优 αA:αT=1.5:1 提示 ContextLoader 可暴露类型权重以在意图模糊时偏向父技能/父域覆盖。规模缺口仍开放：527 工具远低于企业目标的数千级别；架构无关性（8 种 embedding）支持可移植，但未在 ContextLoader 90%+ 上下文节省真正起作用的规模上验证 [5: §Relations]

- **MCP-Zero** [7] (Fei et al., 2025) — 让 LLM 在执行中主动生成 `<tool_assistant>{server, tool}` 结构化请求触发外部检索，替代"全部 schema 注入 prompt"的被动模式。Hierarchical Vector Routing：先按 `server` 字段对 server 描述/Qwen2.5-72B 生成 summary 取较高余弦得分过滤候选 server，再在候选内对 `tool` 字段查工具描述，合成评分 `score = (s_server × s_tool) × max(s_server, s_tool)` [7: §3.1, Eq. 1]。APIBank 多轮 Full 集（Claude-3.5-Sonnet）：标准 schema 注入精度从 Domain 子集 100.00% 跌至 Full 60.22%，MCP-Zero 维持 90.32%；token 6402.2→159.0（−97.52%）[7: §5.3, Table 1]。Needle-in-haystack（1–2,797 工具，最长 248.1k token）：Claude-3.5/Gemini-2.5 上显著超越标准注入；GPT-4.1 因 baseline 已饱和无精度增益（仅省 token）[7: §5.2, Figure 5]。Q.Retrieval 基线（用户查询直接检索）仅 65–72%，作者由此推断"由模型主笔语义对齐请求"是关键收益来源而非检索本身 [7: §5.3, Table 1]。发布 MCP-tools 数据集（308 servers / 2,797 tools，commit ad2d4e6）含原始描述 + LLM 生成 server summary + 预算 OpenAI text-embedding-3-large 嵌入 [7: §4]。关键缺陷：(a) APIBank Full 仅 48 unique tools，"thousands of tools"动机与精度评测断层；(b) "LLM 写、LLM 检"循环——server summary、请求文本、目标工具描述均处 LLM 语义空间，可能放大同空间伪信号 [7: §4.1]；(c) 未与 RAG-MCP/ScaleMCP/Tool2Vec/AnyTool 等已发表工具检索方法对比，仅与 Q.Retrieval 与 Standard Tool Call 两条简单基线对照 [7: §5.3]；(d) §3.2 信息论框架（互信息最大化、O(n)→O(m+k)）从未被实验 instrumented，是装饰性事后合理化 [7: §3.2]。
  - *competes-with* [5] (Agent-as-a-Graph) [high]：同一研究问题（MCP catalog tool retrieval）下两条路径——MCP-Zero 押注"LLM 自描述请求"贴近文档；[5] 押注"显式类型节点 + 类型加权 RRF"消除多跳查询失败模式。[5] 在 LiveMCPBench (527 tools) 上将 MCP-Zero 列为基线并全面胜出（Recall@5 0.85 vs 0.70；nDCG@5 0.47 vs 0.41）[5: §4.2, Table 1]。两者可在工程上组合（MCP-Zero 请求生成 → [5] 类型化检索后端）
  - *builds-on* ReAct (Yao et al., 2023) [high]：`<tool_assistant>` 块本质是 ReAct "Thought→Action" 模式的细化变体——把"Action"再分一层"Tool Request → Tool Schema → Function Call" [7: §2.1]
  - *builds-on* Toolformer / Gorilla / HuggingGPT [med]：将 context-based tool injection 谱系归到这条线上，并把 MCP-Zero 定位为对该谱系"上下文开销不可承受"问题的回应 [7: §2.1]
  - *orthogonal to* [3] (FLARE) [med]：FLARE 关注步进推理在多步规划上的结构性失败，MCP-Zero 关注每步规划已确定后的工具召回；可在同栈共存（planner→请求生成→检索）。MCP-Zero 仅单跳检出测试，未触及多步规划；Figure 3 的 3 步链条由模型 in-context 自然分解，规划深度极浅
  - *orthogonal to* [6] (MAST) [low]：MCP-Zero 解决 FC1 系统设计的子问题（工具 schema 注入上下文压力），未触及 FC2 智能体间错位；候选少则误选少，可间接降低 FM-2.6 推理-动作错位概率，但论文未做该方向的失败模式量化
  - *orthogonal to* [4] (OneFlow) [low]：MCP-Zero 完全在单 agent 范畴内（一个 LLM + 检索器），与 OneFlow 单 LLM 模拟立场一致——是其工具供给侧实现
  - *orthogonal to* [2] (GoA) [low]：GoA 处理推理时多 agent 输出协调，MCP-Zero 处理单 agent 调用前工具供给；层级正交
  - *orthogonal to* [1] (COS-PLAY) [low]：COS-PLAY 协同进化技能库（学得能力），MCP-Zero 在静态 catalog 上检索已有工具（选择能力）；问题域不同
  - *relation to thesis* — 直接命中"工具幻觉的语义解耦防御"研究缺口（thesis §设计上下文 #3）。三点对 Decision Agent 的可操作启示：(a) "模型主笔请求 vs 用户查询直接检索"在精度上重要——APIBank 多轮 Full 上前者 90.32% vs 后者 65.05%，与 BKN "逻辑/行动分离"思想同向，可作为 ContextLoader 前置请求改写器的可行范式；(b) 规模缺口仍在原位——APIBank Full 仅 48 工具，远低于企业目标 5k–10k；2,797 工具的 needle-in-haystack 仅测"能否一次检出 1 个目标工具"，多步、跨域链式检索精度未验证；(c) 强模型上无精度增益（GPT-4.1 在 needle-in-haystack 上对 MCP-Zero 无精度收益，仅省 token），意味着"主动检索"对精度的杠杆主要在弱模型；Decision Agent 若以最强基础模型落地，活跃检索的实际增益更可能落在 token 成本侧而非精度侧——与论题"上下文窗口工程约束"的优先级一致，但需修正"主动检索能提升精度"的过度承诺 [7: §Relations]

> *Emerging sub-cluster: Multi-Agent Failure Diagnostics* — 通过对真实 MAS 执行轨迹做扎根理论编码，建立可量化的失败模式分类法，配套 LLM-as-Judge 实现规模化标注。[6] 不适配 §1.1（Co-Evolving）、图路由子簇、前瞻规划子簇、单智能体充分性子簇或目录层检索子簇——它不提出新架构，而是诊断既有架构为何失败；放置于父桶 §1，建议增设 §1.6，详见 `contradictions.md §Proposed taxonomy extension`。

- **MAST** [6] (Cemri et al., 2025) — 对 7 个开源 MAS 框架在编码/数学/常识基准上的 1642 条执行轨迹做 Grounded Theory 编码，得出 14 个细粒度失败模式分布于 3 大类：FC1 系统设计问题 44.2%、FC2 智能体间错位 32.3%、FC3 任务校验 23.5% [6: §4, Figure 1]。三大头部失败模式为 FM-1.3 步骤重复 15.7%、FM-2.6 推理-动作错位 13.2%、FM-1.5 终止条件无感知 12.4% [6: Figure 1]。关键基准失败率：AppWorld Test-C 86.7%、HyperAgent 74.7%、ChatDev 66.7%、Magentic-One 62%、MetaGPT 60%、AG2 41% [6: Table 1]。三专家在 MAST 上 Cohen's κ=0.88（域内）、0.79（Magentic-One/OpenManus 域外验证）；OpenAI o1 LLM-as-Judge 标注器 κ=0.77 [6: §3.2–3.4, Table 2]。同模型架构差异：MetaGPT vs. ChatDev 使用相同 GPT-4o，FC1/FC2 失败少 60–68% 但 FC3 多 1.56×（ChatDev 显式审阅阶段）[6: §5.1]。同架构模型差异：MetaGPT + GPT-4o 比 + Claude-3.7-Sonnet 少 39% FC1 失败 [6: §5.1, Figure 8]。MAST 指导的针对性干预：ChatDev 角色规约修复 +9.4%、增加高层目标校验步骤 +15.6% [6: §1, Appendix H]。
  - *builds-on* [4] (OneFlow) [high]: 两者并攻"MAS 表现不及预期"——[4] 在同质配置下证明多智能体结构无精度增益，[6] 在所有 7 个 MAS 中诊断出 14 类失败模式；[4] 的单 LLM 模拟器构造性消除 FC2（无智能体间通信），减少 FC1（单一终止点），与 [6] 的失败成因诊断高度互补 [4: §3.1; 6: §1, FC2]
  - *competes-with* [2] (GoA) [med]: GoA 把图式多智能体路由作为性能路径；[6] 在 7 个 MAS 中度量出 FM-2.3 任务偏离 7.4%、FM-2.6 推理-动作错位 13.2%——这两类失败随路由复杂度而扩展。[6] 未直接评测 GoA，但提供分析框架：每条新增路由边都是 FC2 失败的潜在入口 [2: §3.3; 6: §4 FC2]
  - *extends* [3] (FLARE) [med]: [3] 形式化证明步进式推理在长时序任务结构性不足；[6] 的 FM-1.5 终止条件无感知 12.4% 与 FM-3.1 过早终止 6.2% 是规划深度不足在 MAS 执行层的具体表征。[6] 未与规划理论对接，但所有 7 个 MAS 中终止类失败的高发率支持 [3] 的核心论断：瓶颈在规划机制而非通信拓扑 [3: §3.2; 6: Figure 1]
  - *orthogonal to* [1] (COS-PLAY) [med]: COS-PLAY 关注训练时的技能-决策协同进化；[6] 关注运行时失败诊断；操作层级不同，但同属"MAS 不仅是 agents 之和"主题 [1: §3; 6: §1]
  - *orthogonal to* [5] (Agent-as-a-Graph) [low]: [5] 改进调用前的工具/agent 选择机制；[6] 诊断已选定 agents 协同执行时的失败模式；可组合（[5] 提供更准确的 worker 选择 → 减少 [6] 中 FC1 系统设计相关失败的初始概率）
  - *contradicts* thesis §1 (partial, constructive) [med]: 论题主张当前技术栈"workers 之间的协同失真、长链规划退化…仍缺乏学术层面的系统性验证"。[6] 恰好为协同失真维度提供系统性验证——FC2 占 32.3%，并以 1642 轨迹 + κ=0.88 IAA 量化。该结论不反驳论题，但将"未测试的担忧"转为"已度量的患病率"，论题中"缺乏验证"措辞需更新 [6: §4 FC2; thesis §1]

> *Emerging sub-cluster: Heartbeat-Driven Active Scheduling* — 通过周期性心跳信号触发顶层调度器在认知活动（Plan / Reflect / Recall / Dream）间切换，把单 agent 从被动响应升级为自发主动思考；操作维度是"跨步时序频率"，不同于 §1.3（步内规划深度）与 §1.5（调用前选择）。[8] 不适配既有子簇，放置于父桶 §1；建议增设 §1.7，详见 `contradictions.md §Proposed taxonomy extension`。

- **HSC** [8] (Su, 2026, arXiv preprint, single-author) — 提出 Heartbeat Scheduling Controller：在每一 tick `t_k = k·Δt` 读取联合状态 `s_k = [s_k^int, s_k^env]`，由学习策略 `π_θ(s_t, d_t) → z_t` 选择本拍认知活动 `z_t ∈ {Planner, Critic, Recaller, Dreamer, …}` [8: §III, §IV]。双层调度：宏观层 `π_macro` 决定高层认知任务跳转，微观层 `π_micro` 在给定 macro state 下挑选细粒度动作 [8: §III-A]。Dream Mode 在外部任务空闲时由心跳触发，承担三件事：记忆压缩/语义重索引/剪枝、合成经验生成（假设检验+反事实回放+技能复演）、自主目标设定 [8: §IV-C]。状态增强 `s_t' = φ(s_t, h_t)`（其中 `h_t = Encoder(a_{t-k}, …, a_{t-1})`）把近期动作历史嵌入状态以防重复循环 [8: §V-C]。**关键缺陷：理论与实验完全脱节**——§III-IV 通篇是 LLM agent + 可插拔认知模块 + Dream Mode + 自主学习调度的元认知架构，§VI 实验却是 LSTM 在合成 1,800 天 × 24 小时 × 6 类动作序列上的监督 next-action 预测，没有 LLM、没有任何认知模块的实例化、没有 Dream 触发、没有策略梯度/RL/online learning 步骤 [8: §III-IV vs §VI]。所谓"成功"指标（动作覆盖 6/6、预测分布熵 2.31 vs 真值 2.35 bits、稀有动作 recall 78.3%）实际是模型反向工程作者写在数据生成器里的概率规则，且对任何 baseline（ReAct/Reflexion/AutoGPT/朴素小时频率分类器）均无对比 [8: §VI-A.1, §VI-A.3]。扩类实验（加入"Recalling What is Important"为动作 6）仅给出单日定性图，预测时间错位 4 小时（真值 12 时 → 预测 16 时），无测试集聚合 [8: §VI-B.3]。
  - *competes-with* [6] (MAST) [med]：MAST 的 FM-1.3 步骤重复 (15.7%) 与 FM-1.5 终止条件无感知 (12.4%) 都是"何时停 / 何时再思考"的调度问题；HSC §V-C 状态增强正面对应 FM-1.3，HSC 心跳触发反思正面对应 FM-1.5。但 MAST 基于 1642 真实 MAS 轨迹（κ=0.88），HSC 基于合成 LSTM 实验（无 baseline）——两者在"调度问题真实性"上互证，但 HSC 未将 MAST 失败模式作为评测靶点 [8: §V-C; 6: §4]
  - *orthogonal to* [3] (FLARE) [med]：FLARE 改造单次推理深度（前瞻 MCTS），HSC 改造跨次推理频率（心跳调度）；操作层级不同（步内 vs 跨步），可叠加；HSC 未触及 FLARE 形式化的"贪心策略局限" [3: §3.2; 8: §IV]
  - *orthogonal to* [7] (MCP-Zero) [low]：MCP-Zero 主动驱动工具检索（调用前），HSC 主动驱动认知活动调度（跨步时序）；同属"主动驱动"思路但层级互补，本论文不涉及工具
  - *orthogonal to* [4] (OneFlow) [low]：HSC 在单 agent 范畴讨论调度，与 OneFlow 单 LLM 立场一致——是其时序供给侧的一个延伸方向（但未被 [4] 引用）
  - *orthogonal to* [1] (COS-PLAY), [2] (GoA), [5] (Agent-as-a-Graph) [low]：问题域不同（技能进化 / 多 agent 输出协调 / 工具检索）
  - *builds-on* Reflexion (Shinn et al., 2023), Self-Refine (Madaan et al., 2023), ReAct (Yao et al., 2023) [high]：[8] §II-A/B 显式将三者列为"failure-triggered 或 task-completion-triggered"反思代表，把 HSC 定位为 proactive、心跳周期驱动、可在空闲时介入的扩展
  - *extends* ACT-R / SOAR [med]：[8] §II-D 显式引用并主张"functional 而非 structural"——保留人脑认知模块（Planner/Recaller/Dreamer）但放弃产生式规则结构；模块未在实验中实例化
  - *relation to thesis* — 直接命中"常驻 Runtime 的学术对照"研究缺口（thesis Design Context goal 2: Heartbeat + Cron + Reflection），但作为**反证案例**而非正面支撑：本论文是迄今唯一直接以"heartbeat scheduling"为标题的 LLM agent 工作，其实验完全不验证主张，揭示**"主动调度学术稀缺是真实的——稀缺到首篇相关论文实质未做相关实验"**。与 thesis 预判一致（"Heartbeat+Cron 学术稀缺，遇到必须收"），但同时降低"通过文献合成获得设计指南"的预期。可吸收的语言资源：HSC 概念命名（macro/micro 双层调度、Dream Mode 三大功能、复合奖励 `R_total = α·R_external + β·R_internal`）、状态增强 `s_t' = φ(s_t, h_t)` 防重复机制（即便实验未验证，作为 Dolphin 状态机引入近 k 步动作哈希以缓解 MAST FM-1.3 的形式化参考）。证据强度全文 [low]——单作者、未同行评议、合成数据、无 baseline、理论与实验脱钩；不应被后续合成误用为"主动调度有效"的引用 [8: §III-IV vs §VI; thesis Design Context goal 2]

> *Sub-cluster join: Heartbeat-Driven Active Scheduling (§1.7)* — ProAct 与 [8] HSC 同属"空闲态主动准备"操作维度（idle-time compute → 主动认知/检索），作为 HSC 的**严格评测对照版**加入该子簇而非另立新桶。若反审认为"prediction-guided idle-time compute"与"heartbeat tick scheduling"机制差异足以独立成簇，见 `contradictions.md §Proposed taxonomy extension`。

- **ProAct** [20] (Hu et al., 2026, 上海交大 + 腾讯) — 用 idle-time compute 在交互间隙预测并预取"可能的未来用户需求"，两模块紧耦合：Future-State Prediction（local scenario / related expansion / memory-gap augmentation 三来源生成候选 Zt）+ Idle-Time Acquisition（value gate S(z)=wr·rz+wg·gz+wv·vz+wτ·τz 评分 → S(z)≥θval 才即时检索 → artifact 写回 memory，并按 {push, queue, store} 三态投递）[20: §3.1-3.4]。ProActEval（200 场景 / 40 域）：Directed Idle 相对 Reactive 把 T100 降 14.8%、User Effort 降 11.7%、Hallucination Rate 降 28.1%（0.132→0.095），Anticipation Recall 0.000→0.428，但额外消耗 111.8k active token/场景 [20: §1, Table 2]。**消融揭示增益来自"预测方向"而非"后台搜索本身"**：Undirected Idle（有 acquisition 无 prediction）花 69.8k token 仅把 T100 降 0.9%、Anticipation Recall 保持 0；改造自公开 ProactiveAgent 的 GPT-4o prompting baseline 在 1,572 可预测需求中只命中 32 个（Recall 0.020）vs ProAct 命中 703 个（0.447）[20: §5.1, Table 2-3]。MemBench reflective participation SOTA：10k token reflective accuracy 0.843（最佳先前 baseline 0.742）[20: §5.2, Table 14]。搜索预算 k 是 operating point 而非应最大化参数：k 4→16 使 Recall 0.253→0.432 但 T100/UE 趋平 [20: §5.3]。**关键缺陷：** (a) "idle compute is free/wasted"动机与 111.8k token 实际开销直接冲突，未对成本定价；(b) −28.1% hallucination 是 closed-world fact sheet 提前检索的产物，绝对仅 3.7pp（Fact Accuracy 本已 0.972），不可外推开放世界；(c) value/delivery gate 全为手工常数（四权重各 0.25 / θval=60 / push 阈值 40/70），标题含"and Learn"但被学习的只是 memory 内容、gating policy 零参数优化；(d) 跨两套评测换模型（proactivity gpt-4o / memory Qwen2.5-7B），端到端链路断裂；(e) simulator + 被测 + judge 同源 gpt-4o 族，无第三方标注；(f) 自主预测未来需求在架构层无授权/治理出口——delivery gate 的 Cost 度量"中断成本"而非"是否被授权研究该主题"；(g) 预测器对 benchmark `predictable_after` 链可能过拟合，真实对话可预测性无外部锚点 [20: §1 vs Table 2, §4.1, Appendix C/O]。
  - *builds-on / competes-with* [8] (HSC) [high]：thesis goal 2 同一问题、两种证据强度。HSC 提出心跳触发 Dream Mode 做"空闲态自主认知活动"但实验是合成 LSTM next-action 预测、零 LLM、零 Dream 实例化、无基线（证据为零）；ProAct 覆盖几乎同一空闲态用途（memory 维护 / 证据预取 / 未来需求预测），并补齐 HSC 缺失的全部东西：200 场景 benchmark + Reactive/Undirected/ProactiveAgent 三类基线 + bootstrap CI + MemBench SOTA。builds-on（继承"idle = 主动准备机会"范式）兼 competes-with（用 prediction-guided gating 取代 HSC 的 intrinsic goal generation，给出可证伪工程实现）。ProAct 的存在使 thesis "goal 2 学术稀缺"判断需更新：稀缺的是"被严格评测的"主动 Runtime，而非"主动 Runtime 的想法" [20: §5.1; 8: §III-IV vs §VI]
  - *contradicts* [19] (PAGRL) [high]：在"主动行为是否需要 action provenance 治理出口"上正面冲突。ProAct 以"无需用户预定义任务或日程、自主推断未发声需求"为卖点（§2）——正是 thesis（由 [8] 触发、[19] 强化的决议）禁止的"intrinsic goal generation 进入生产路径"；PAGRL 要求每个 consequential action 前必经 deliberation，无显式授权来源的 intent 应 ESCALATE。结构性映射：ProAct {push, queue, store} 三态可被 PAGRL 治理化——store（静默进 memory，无外部副作用）≈ 低治理风险可自主，push（主动通知 / 触发外部动作）≈ 必经 ESCALATE/HITL。ProAct 是"未治理主动端"的具体参照，[19] 提供其治理改造模板 [20: §2, §3.4 vs 19: §4.1, §4.4]。详见 `contradictions.md §Contradiction (cross-paper)`
  - *builds-on* [17] (FadeMem) [med]：ProAct memory-gap augmentation（memory 维护识别 stale/incomplete/weak/missing → 转为主动 acquisition 目标）与 FadeMem 生物启发遗忘/衰减是同一记忆生命周期的正反两面——FadeMem 决定"哪些记忆该衰减剔除"，ProAct 决定"哪些 gap 该主动补全"；两者共同构成 thesis goal 5"记忆不无限膨胀 + 经验继承"所需的双向 lifecycle [20: §3.3 vs 17]
  - *competes-with* [18] (Governed Memory) [med]：两篇都主张"memory + provenance"但治理深度截然不同——[18] 把治理（governance variable / schema / append-only audit）作记忆层一等公民；ProAct provenance 只是 artifact 上的"证据来源"元数据，不承载授权 / 访问控制 / 审计语义。ProAct 的 memory write-back 路径缺 [18]/[19] 的 pre-action 治理检查，须被 [18] 式治理 schema 包裹后才能进生产 [20: §3.4 vs 18: §1, §3]
  - *competes-with* [6] (MAST) [low]：ProAct 是单 agent 时序范式，不直接面对 MAST 多 agent 失败分类；其 −28.1% hallucination 若被用作"主动准备降低幻觉"证据，须与 FM-2.6 / FM-3.3 对照——该幻觉下降来自封闭 fact sheet 提前检索，无法证明减少多 agent 长链任一类 MAST 失败 [20: Table 2 vs 6: §4]
  - *orthogonal to* [7] (MCP-Zero) [low]：均"主动 vs 被动"但层级不同——ProAct 决定"idle 时研究什么主题"，MCP-Zero 决定"研究阶段如何写工具请求"；可叠加，论文互不触及 [20: §3.3 vs 7]
  - *relation to thesis* — 直接命中 goal 2（Heartbeat + Cron + Reflection 主动 Runtime）与 goal 5（跨会话层级化 Memory），并影响"学术稀缺=机会窗口"与"Decision Agent 与 OpenClaw 边界判据"两条可证伪点。(a) **goal 2 [high]**：ProAct 是 HSC 缺席的"被严格评测的主动 Runtime"，但落在 thesis 明确拒绝的"未治理主动端"——采纳其机制（Future-State Prediction + memory-gap augmentation 作 Reflection 建议生成层算法骨架）、改造其触发（intent 来源从"自主推断"改为 cron / 用户授权 / Composer plan 派生）；{push, queue, store} 三态天然对齐治理分轨（store 可自主 / push 必经 [19] PAGRL ESCALATE + HITL）；ProAct 缺的 TraceAI-first 须补（provenance 仅记"检索了什么"，须扩为"谁授权 + 先写审计再执行"）。(b) **修订可证伪点"学术稀缺=机会窗口"[high]**：此前被 [8] 部分证伪为"可由 cron + ReAct + 治理围栏简单解决"；ProAct 提供反向证据——Undirected Idle（≈ 朴素 cron 触发 ReAct 背景探索）花 69.8k token 仅得 ~1% 增益，"预测方向"才是真增益。该点应更新为：主动需求预测不是 cron+ReAct 可平凡解决的（[8] 推断被部分反驳），但学术解法昂贵（111.8k token/场景）且未治理；工程取舍是"治理边界内做有限预测"。(c) **OpenClaw 边界判据 [med]**：ProAct §2 显式把 OpenClaw/Hermes 归为"always-on 但主动行为仍由用户指定 schedule 触发"，把自身定位为"完全自主推断"——给 thesis 边界判据增加一条触发自主性轴：OpenClaw（低自主）— Decision Agent 治理边界内（中自主全治理）— ProAct（高自主无治理）；差异化 = 用 ProAct 预测机制 + OpenClaw 显式触发约束 + [18]/[19] 治理底座组合。(d) **goal 5 [med]**：memory-gap augmentation 为"双向记忆 lifecycle"补全主动写入端，增量抽取管线（profile_updates / updated_summary / key_info / extracted_facts）为 build_memory/search_memory 提供字段参考；但 ProAct 是扁平 User 级记忆，无 Role/Org 层级、无 [10] 访问控制，须被层级化 + 治理 schema 包裹。证据强度整体 [med]（相比 [8] 的 [low] 强得多：benchmark + 双消融 + bootstrap CI + 公开基线对比），但 closed-world 合成 benchmark + 同源 judge + 换模型 + gating 手工常数 + 自主 intent 无治理出口——作为机制骨架与评测协议参考足够，作为"企业场景净收益"证据不充分 [20: §Relations; thesis goals 2, 5]

> *Emerging sub-cluster: Broadcast-and-Self-Select MAS Communication* — 主智能体不向特定 sub-agent 派发任务，而是把请求广播到共享 blackboard β，helper agent 自判是否响应并写回独占响应板 β_r；helper 间互不可见。操作维度是"通信拓扑"，与 §1.2（GoA 推理时图路由）、§1.5（调用前类型化检索）、§1.6（失败诊断）、§1.7（时序调度）层级均不同。[9] 不适配既有子簇，放置于父桶 §1；建议增设 §1.8，详见 `contradictions.md §Proposed taxonomy extension`。

- **Blackboard MAS** [9] (Salemi et al., 2026) — 把数据科学问答任务的多智能体通信范式从 master–slave 替换为 blackboard：主智能体（ReAct，T=10 步）在 5 动作集（plan/reason/run_code/request_help/answer）中以 `request_help` 写入共享 blackboard β；每个 cluster 的 file agent 与 search agent 自判可否响应，能则写入独占响应板 β_r，主智能体并行回灌 β_r 后选择"使用或忽略" [9: §3.1, §3.2 + footnote 2]。**端到端：** 在 KramaBench / DSBench-filtered / DA-Code-filtered（合计 ~448 题）× 4 backbone（Qwen3-Coder-30B / Gemini 2.5 Flash & Pro / Claude 4 Opus）上 macro 平均较最强基线（Master–Slave）相对提升 13%–57%；Claude 4 Opus + Blackboard 取 31.43% [9: §4.2 Table 1]。**File discovery（Gemini 2.5 Pro）：** Blackboard F1 0.561 vs Master–Slave 0.513 vs RAG 0.247 [9: §4.2 Table 2]。**Scalability claim：** 在数据湖规模（45–1556 文件）与 Blackboard 相对增益间观测正相关（DA-Code 145 文件 +70%、Astronomy 1556 文件 +211%）——但 Astronomy Master–Slave baseline 仅 3.16%–11.52%，分母极小放大相对增益 [9: §4.2 "Scalability Comparison" + Appendix F]。**Cost：** runtime 132–145 秒/题与 RAG/Master–Slave 相差 < 10%，但 USD/题 ≈ 2.3× RAG / 1.8× Master–Slave [9: §4.2]。**关键缺陷：** (a) "Helper agent autonomy" 实质是 prompt 二选一判断，无分布式控制 / 投票 / 仲裁协议——是命名修辞而非架构属性 [9: §3.2 + Appendix C Figure 6]；(b) 多 helper 响应冲突时主智能体仲裁机制完全不透明，case study 显示 8 helper 中 3 个同时响应 [9: §3 + Appendix E]；(c) 主实验默认聚类（Gemini-2.5-Pro 文件名）被自家消融实验证明劣于 E5-Large 内容嵌入 + KMeans，但未用最佳配置重做主结果 [9: §4.2 vs Table 1]；(d) Master–Slave baseline 是 ReAct 主智能体 + 指名调用，比 LangGraph supervisor / CrewAI hierarchical / OneFlow 单 agent 折叠都弱，"Blackboard > Master–Slave" 不等价于 "Blackboard > 工业最佳实践" [9: §4.1]；(e) DSBench / DA-Code 经手工筛除"无 hint 题目"（253q / 91q），无 inter-rater agreement，外部不可比；(f) DSBench 用 Gemini 2.5 Pro 既做 backbone 又做 LLM-as-Judge，无交叉 judge [9: §4.1 + Appendix A]；(g) cluster 上限 27 / k≤16，远低于企业 5k–10k 规模——broadcast 成本随 helper 数线性放大，论文从未触及 [9: §3 Table 3]；(h) offline file-content 读取与分析成本被均摊到所有题目，未做摊销分析 [9: §3.2 vs §4.2]。
  - *competes-with* [6] (MAST) [med]：MAST 把 master–slave 类 MAS 的协调失败归类为 FC2 Inter-Agent Misalignment（占 32.3%）并量化 14 个 FM 的患病率；[9] 在设计层面给出 master–slave 的替代方案（broadcast + 响应隔离 β_r），主张缓解 FM-2.1 信息隐瞒 / FM-2.4 信息共享错误 / FM-2.6 推理-动作错位。但 [9] 未引用 MAST、未把 14 个 FM 作为评测靶点；"Blackboard 缓解了哪些 FM / 引入了哪些新 FM" 是空白。两者在"master–slave 是结构性瓶颈"上互证，方法论 competes（设计修复 vs 失败分类）[9: §3 footnote 2; 6: §4 FC2]
  - *competes-with* [4] (OneFlow) [med]：OneFlow 论证同质 MAS 可被折叠为单 LLM 顺序执行——但前提是任务不超出单 LLM 上下文。[9] 的 DS-GRU baseline（"把所有文件塞 prompt"，对应 OneFlow 的 folded MAS）在 Qwen3 上仅 1.21%、在 Claude 4 Opus 上仅 4.11%——明确**反证** OneFlow 在"上下文必然溢出的数据发现"场景下的折叠主张。两者并不矛盾，而是边界条件互补：OneFlow 适用于上下文充足任务，Blackboard 适用于上下文必然溢出场景。[9] 未引用 OneFlow [4: §3.1; 9: §4.2 Table 1]
  - *builds-on* [7] (MCP-Zero) [med]：MCP-Zero 让模型主笔结构化请求驱动**工具检索**；[9] 让主智能体在 blackboard 写结构化请求驱动**文件/信息发现**。两者共享"模型主笔请求 + 语义路由"骨架，但层级不同（工具召回 vs 文件发现）。MCP-Zero 已揭示强模型 baseline 饱和——[9] Claude 4 Opus + Blackboard 仅 macro 31.43%，远未饱和，说明"数据发现"难度结构不同于"工具检索"
  - *builds-on* [5] (Agent-as-a-Graph) [med]：Agent-as-a-Graph 用类型化 KG 做 typed retrieval（Recall@5 +14.9%）；[9] 用 LLM 文件名聚类做 cluster-typed routing。同向"用结构化分区改善 flat retrieval"，但 [9] 聚类基于命名相似性而非显式 schema，类型化弱于 [5] 的 BKN 同构思路。[9] "内容嵌入 + KMeans 优于命名聚类"消融与 BKN 类型化思路是叠加而非冲突的工程证据
  - *orthogonal to* [3] (FLARE) [low]：[9] 主智能体仍是 ReAct（按 FLARE 是贪心策略），但 [9] 任务核心难点是"信息检索 + 分区路由"而非"长链规划"，FLARE 适用度低；两者层级不同（步内推理深度 vs 跨智能体通信结构）
  - *orthogonal to* [2] (GoA), [8] (HSC), [1] (COS-PLAY) [low]：问题域不同（输出协调 / 时序调度 / 技能进化）
  - *relation to thesis* — 同时命中 design goal 3 (Shared Workspace) 与 goal 4 (Composer)，并对治理优先核心定位提出张力：(a) **Blackboard ≅ Shared Workspace 的工程化原型 [med]**（单源设计对应，corroboration pending；helper 上限 27，企业规模未独立复现）：β / β_r 分离 + helper 互不可见为 thesis goal 3 提供候选 default 设计——Decision Agent Shared Workspace 可考虑"按需广播 + 隔离响应"两条窄通道，但 5k–10k 规模下 broadcast 线性成本未验证，需保留 BKN 预过滤回退路径；并注意 devil's-advocate 已指出"helper autonomy"在 [9] 中实质是 prompt 二选一判断而非分布式协议，"广播 + 自荐" 与 "中心 BKN capability lookup" 谁更鲁棒在该论文中未对照；(b) **Composer "无中心能力注册表" 设计获得初步支持 [med]**：4 backbone × 3 数据集一致表明"主 agent 不必精确知道 sub-agent 能力 detail" 工程上可行，可作为 Composer Plan 节点"语义请求 + 候选 Agent 自荐"机制的依据，避免向通用 DAG 编排漂移；(c) **治理张力 [med, 设计警告]**：β_r 私有阻断 helper 响应进入跨 agent trace 视图，与 thesis "TraceAI-first / action provenance must be explicit" 不直接对齐——Decision Agent 落地 Shared Workspace 时需补"β_r 私有 + 全量 trace 镜像到 TraceAI"，使治理审计独立于工程隔离；(d) **可证伪点更新**：thesis 表中 "Shared Workspace 能显著降低 FC2 协同失真" 状态从"未验证"更新为"间接工程证据，缺直接失败模式量化"；新增可证伪点 "Composer 无能力注册表设计在企业 5k–10k Agent 规模仍成立"——本论文 helper 上限 27，证据仅适用 ≤30 helper 小规模 [9: §3 footnote 2; thesis goals 3, 4]

> *Emerging sub-cluster: Cross-Session Layered Memory with Access Control* — 跨用户多 agent 长期 memory 共享 + 时变 bipartite 授权图 (G_UA / G_AR) + provenance-aware fragment 过滤；操作维度是"跨会话持久化记忆 + 权限治理"，与 §1.8 (Blackboard 任务执行期通信结构) 在隔离语义上同构（M_private↔β_r 私有、M_shared↔β 共享），但时间尺度不同（任务期 vs 跨会话）。[10] 不适配既有子簇，放置于父桶 §1；建议增设 §1.9，详见 `contradictions.md §Proposed taxonomy extension`。

- **Collaborative Memory** [10] (Rezazadeh et al., 2025) — 把单用户 monolithic memory 替换为带访问控制的双层 memory（M_private 按贡献用户隔离 + M_shared 按贡献 agent 分组）。每条 fragment 携带不可变 provenance 四元组 (T, U, A, R)——时间戳 / 贡献用户 / 贡献 agent 集合 / 创建时使用过的 resource 集合 [10: §3.2]。时变授权由两张 bipartite graph 刻画：G_UA(t) ⊆ U×A 用户-agent 授权、G_AR(t) ⊆ A×R agent-resource 授权；可读 fragment 集合 M(u,a,t) = {m | A(m) ⊆ A(u,t) ∧ R(m) ⊆ R(a,t)}——只有 fragment 所涉 agent 与 resource 当前都仍在该用户授权内才可见 [10: §3.1, §3.2 Eq. 3]。Read / Write 是两条独立 policy，可在 global / per-user / per-agent / per-time 四个粒度独立配置；本文实验取 simple read（top-k embedding 相似）+ transformation write（LLM 系统级 prompt 做 redact / anonymize / paraphrase）[10: §3.4, §4]。**S1 全协作（5 用户 / 6 agent / 6 KB / MultiHop-RAG 2,556 题）**：collaborative accuracy ≥ 0.90 与 isolated 相当；overlap 50% 时 resource 调用降 **61%**、75% overlap 降 **59%** [10: §5.1, Figure 3]。**S2 非对称授权（4 用户 / 4 agent / 200 业务查询，仅 Strategy Director 可见全部 agent）**：4 角色 resource utilization 直方图整体左移；Strategy Director 由于已吸收前置 fragment 收益最大；无 ground truth、无具体百分比汇总 [10: §5.2, Figure 4]。**S3 动态访问图（5 用户 / 5 RAG agent / 5 科学领域 / 9 时段 / 100 查询，先 0→25 边再吊回）**：accuracy 与 agent 调用数随授权扩张 / 吊销同步起伏 (峰值 ~0.62)；resource 调用数整体下降，归因为 fragment 复用替代外部检索；访问矩阵（Figure 6）零越界 [10: §5.3]。**关键缺陷：** (a) "immutable provenance" 是声明而非机制——实现层只是普通 metadata，无签名 / 哈希链 / append-only log，任一系统内部组件 bug 即可静默修改 provenance [10: §3.2 vs §4]；(b) 安全保证依赖 LLM 概率输出——redact / anonymize 由 LLM prompt 完成但**无 redaction 失败率评测**，§6 自陈"occasional hallucinations or policy breaches" [10: §4 Policy Instantiation, §6]；(c) 跨用户 fragment 冲突合并机制完全缺失（同一事实矛盾写入无版本号 / 投票 / 仲裁）[10: §3 整章]；(d) 无 decay / TTL / pruning，memory 单调增长 + top-k 检索精度退化曲线缺失 [10: §3.4, §4]；(e) coordinator LLM (gpt-4o) agent selection 失败模式无独立评测，与 [6] MAST 14 FM 完全无对应 [10: §4]；(f) Bipartite 图规模仅 5 用户 × 6 agent × 6 resource，远低于企业 5k–10k 工具 / 数百 agent / 数百用户角色目标（差 2–3 数量级）；Eq. 3 求交计算在大规模下的索引设计未触及 [10: §3.2]；(g) 未与 RBAC / ABAC + flat memory baseline 对比，"bipartite-graph + provenance" 的相对增量未隔离量化 [10: §2 vs §5]；(h) S2 资源使用降幅未给具体百分比；S2 的 200 条业务查询由 GPT-4o 合成、coordinator/agent/aggregator 也是 GPT-4o，无 cross-model sanity check；S1 LLM-as-Judge 与 backbone 同 gpt-4o，无 cross-judge 复评 [10: §5.2, §D.1, §4]。
  - *builds-on* [9] (Blackboard MAS) [high]：β / β_r 双板分离与本文 M_shared / M_private 双层 memory 在结构上同构——β ↔ M_shared 都是跨主体可见共享通道、β_r ↔ M_private 都是按"贡献者"隔离的私有空间。两文角度互补：[9] 处理任务执行期多 agent 通信结构（按任务划分），[10] 处理跨任务跨用户长期 memory 累积（按用户划分 + 时变授权）；本文为 [9] 的 β_r 私有性补上**长期持久化 + 重访权限校验**机制，是 [9] 在跨会话维度的自然扩展 [9: §3 footnote 2; 10: §3.2]
  - *competes-with* [9] (Blackboard MAS) [med]：但两者在"helper / agent 之间是否相互可见"上对应不同设计选择。[9] 强制"helper agent 互不可见"防执行期 cross-influence；本文 shared memory 中"agent 互相产出的 fragment 可被其他 agent 读到"——若 agent 在为 user A 服务时读到 agent B 为 user A' 写的 fragment，则跨 agent 信息流是允许的（仅经权限校验）。Decision Agent 落地 Shared Workspace 时必须在两者间显式选择 [9: §3.2; 10: §3.2]
  - *competes-with* [6] (MAST) [med]：collaborative memory 在设计层面对若干 FM 是潜在缓解——FM-2.1 信息隐瞒（shared memory 让先前结果跨 user 可见）、FM-2.4 信息共享错误（write policy 强制结构化转写降低自由对话漂移）。但**论文未引用 MAST、未做失败模式量化**——"我修复了 FC2 中哪几条"完全空白；不能作为 FC2 缓解的实证证据 [6: §4 FC2; 10: §5 整章]
  - *competes-with* [4] (OneFlow) [low]：OneFlow 处理空间结构（agent 数量），本文处理时间维度（用户跨会话）；论域错位但不冲突——即使把多 agent 折叠为单 LLM，多用户 memory 隔离 / 共享 / 权限约束仍独立存在。两者并行可叠加 [4: §3.1; 10: §3 整章]
  - *builds-on* [1] (COS-PLAY) [med]：[1] 关注 skill 库随对话演化（skill = 可复用决策技能），本文关注 memory fragment 库随多用户交互演化（fragment = KV 形式事实/总结）。两者结构同构——一个不断增长的可复用知识库 + 检索增强；本文引入了 [1] 缺失的 access control 与 provenance 维度 [1: §3; 10: §3.2]
  - *builds-on* [5] (Agent-as-a-Graph) [low]：[5] 用 KG 类型化 + 图查询做工具召回（Recall@5 +14.9%）；本文用纯 cosine similarity top-k 做 fragment 召回，**无任何结构化类型 / KG**。但 fragment 的 provenance (U, A, R) 元组本身就是天然"类型化标签"，可与 [5] 的 typed retrieval 叠加（先按 provenance 类型过滤、再做 embedding 相似度）——本文为 [5] 提供"类型来源"的现成结构 [5: §3; 10: §3.2]
  - *builds-on* [7] (MCP-Zero) [low]：MCP-Zero 让模型主动写结构化请求驱动工具检索；本文 write policy 让 LLM 主动按 prompt 转写 fragment 为结构化 KV。两者共享"模型主笔结构化输出"骨架，但层级不同（工具召回 vs memory 写入）[7: §3.1; 10: §4]
  - *orthogonal to* [3] (FLARE) [low]：FLARE 处理单 agent 推理深度，本文处理多用户 memory 共享，论域不重叠；但本文 coordinator + agent + aggregator 整体仍是步进推理，FLARE 局限若被用于长链规划场景仍适用
  - *orthogonal to* [2] (GoA), [8] (HSC) [low]：问题域不同（单 task 内输出协调 / 跨步时序调度）
  - *relation to thesis* — 直接命中 thesis Design Context 的 **goal 5（跨会话层级化 Memory）** 与 **goal 3（Shared Workspace 的 ISF 权限控制维度）**——是当前 corpus 中**第一份**直接处理"多用户 memory 共享 + 时变访问控制"的工作。可操作影响：(a) **三层记忆原型 [med]**：thesis 需 User / Role / Org 三层，本文给 User / Shared 两层缺 Role 中间层；但 M_shared(a, t) 按贡献 agent 分组实质是 Role 维度雏形——Decision Agent Role 层可类比为"按贡献 agent 集合切分的 shared memory 子区间"，再加 Org 层作为第三层；(b) **写入双通道直接同构 [high]**：π^{write/private} 与 π^{write/shared} 双 policy 与 thesis "private 直写 + shared 经审 / 转写"完全对齐，Table 1 两条系统级 prompt（private 保留具体细节、shared 强制移除用户特定信息）是双通道 prompt 设计的可直接借鉴起点；(c) **provenance ↔ TraceAI 对齐 [high]**：(T, U, A, R) 四元组与 thesis "TraceAI-first / action provenance must be explicit" 结构上完全对齐——每条 fragment 携带"何时 / 谁的指令下 / 哪些 agent 参与 / 调了哪些 resource"恰是 TraceAI 审计维度；但本文 provenance 是普通 metadata 无防篡改，Decision Agent 借鉴时**必须升级为 append-only + 哈希链 / 签名**才满足"事后审计可信"治理要求；(d) **ISF 权限模型形式化 [high]**：G_UA(t) 与 G_AR(t) 双 bipartite graph 是 ISF 权限模型的具体数学形式，Eq. 3 的 M(u,a,t) 求交规则给出"在两层授权下何条 memory 可见"的可计算判据；(e) **关键工程空缺 [high]**：thesis 治理与遗忘机制 / 跨用户冲突合并 / Role 层细分 / provenance 防篡改 ——这四项 Decision Agent 必须在借鉴本文之外**自行补齐**；(f) **新增可证伪点（建议加入 thesis 表）**：(f1) "User/Role/Org 三层记忆设计在 5k+ 工具 / 数百用户角色规模下，Eq. 3 求交计算保持亚线性"——本文最大配置 5×6×6，证据仅适用数十节点；(f2) "LLM-based write policy（redact / anonymize）在企业敏感数据场景下失败率 ≤ 1%"——本文未做该评测，>5% 泄漏率即整个 transformation write 路径不可接受、需替换为规则引擎。证据强度全文 [med]：方法论清晰、形式化干净、3 场景递进合理；但实验规模小、provenance 无防篡改、安全保证依赖 LLM 概率、跨用户冲突未处理、无衰减、关键 ablation 缺失——**应作为 goal 5 access control / provenance 形式化原型来源，不应作为 LLM-based redaction 在企业治理场景下安全性的直接证据** [10: §Relations; thesis Design Context goals 3, 5]

> *Emerging sub-cluster: MAS Cross-Trial Self-Evolution via Hierarchical Memory* — 通过三级图结构将 MAS 跨轨迹协作经验蒸馏为层级记忆（交互图 / 查询图 / 洞察图），并按 agent role 过滤注入，把 MAS 从"每次从零出发"升级为"从历史积累认知"；操作维度是"跨任务 episode 经验蒸馏 + role 特化注入"，与 §1.9（跨会话权限治理）在解决持久记忆问题上互补——[10] 解决"谁能访问"，[16] 解决"如何从历史学习"。[16] 不适配 §1.9（访问控制是 [10] 的核心，[16] 无授权机制），放置于父桶 §1；建议增设 §1.12，详见 `contradictions.md §Proposed taxonomy extension`。

- **G-Memory** [16] (Zhang et al., 2025, NUS/Tongji/UCLA/A*STAR/NTU) — 三层图层级记忆系统，用于 MAS 跨轨迹自演进：*交互图*（任务执行期 utterance 节点 + 时序边）→ *查询图*（query 节点 + 语义边，记录成功/失败状态 + 交互图指针）→ *洞察图*（insight 节点 + 超边，表示"insight 经由 query 相互语境化"关系）[16: §3, §4]。检索管道：MiniLM 余弦相似度从查询图 top-k 粗检 → 1-hop 邻居扩展 Q̃S → 上行遍历（query→insight）提取泛化洞察 IS → LLM 提取器下行遍历（query→interaction，Eq. 7 相关性评分）提取核心子图 → 按 agent role 过滤后注入各 agent Memi（算子 Φ）[16: §4.1, §4.2]。**端到端主结果：** MacNet + Qwen-2.5-14b 在 ALFWorld 上较最强基线 +20.89%；AutoGen + Qwen-2.5-14b 在 HotpotQA 上 +10.12%；跨 3 框架（AutoGen/DyLAN/MacNet）× 3 backbone（GPT-4o-mini/Qwen-7b/14b）× 5 基准一致超越无记忆基线，无一基准低于无记忆版本 [16: §5.2, Table 3]。**消融：** 移除 interaction 子图 −4.47%/−3.82%（AutoGen/DyLAN）；移除 insight −3.95%/−3.39%——两层各自有不可替代贡献 [16: §5.4, Figure 4c]。**跳数敏感性：** 1-hop 最优（AutoGen PDDL 55.24%），2-hop 退化至 49.79%——无关 insight 在 2-hop 传播时主导噪声 [16: §5.4, Figure 4a]。**Token 效率：** AutoGen + Qwen-14b PDDL 额外 1.4×10⁶ token / +10.32%，优于 MetaGPT-M 2.2×10⁶ token / +4.07% [16: §5.3, Figure 3]。**单 agent 记忆 naive 迁移 MAS 有害：** Voyager 将 AutoGen PDDL 降低 4.17%，MemoryBank 将 DyLAN ALFWorld 降低 1.50%——因无法按 agent role 提供特化记忆线索 [16: §5.2, Tables 1–3]。**关键缺陷：** (a) 5 个基准均为实验室场景，未触及企业多步工具调用 AutoFlow [16: §5.1, §6]；(b) 三层图无 TTL/decay/pruning/冲突合并机制，长期质量退化曲线缺失 [16: §4.3, §6]；(c) insight 由 LLM 自动生成，无忠实度评测，幻觉 insight 通过超边污染未来检索 [16: §4.3, Eq. 10]；(d) 1-hop 最优但 2-hop 退化说明 graph 边质量对噪声敏感，无剪枝机制 [16: §5.4]；(e) Role-specific 注入算子 Φ 实现委托 Appendix C，未独立 ablation [16: §4.2, Eq. 8]；(f) 未与 MAST 14 FM 对应——"G-Memory 提升性能"是聚合黑盒主张，失败模式缓解机制未量化 [16: §5 整章]。
  - *extends* [10] (Collaborative Memory) [med]：两者均处理 MAS 跨主体持久记忆——[10] 的 M_shared/M_private 双层与 [16] 的 interaction/query/insight 三级在关注点上互补：[10] 解决"谁能访问"（时变 bipartite 授权图），[16] 解决"如何从历史学习"（跨轨迹蒸馏 + role 过滤注入）；[16] 的 insight 层可作为 [10] M_shared 的自演进写入来源，[10] 的 provenance 四元组可作为 insight 节点的不可变标签——两者组合是 goal 5 完整架构的候选，但均未引用对方，结合是合成推断 [10: §3; 16: §3]
  - *competes-with* [10] (Collaborative Memory) [med]：在"inter-agent 信息流可见性"上存在设计分叉——[10] 允许 agent 跨用户共享 fragment（经权限校验），[16] 通过 role-specific Φ 过滤仅将相关历史注入特定 agent；Decision Agent 落地 Shared Workspace 时需显式选择"agent 间记忆流是开放共享（[10] 路径）还是按 role 隔离（[16] 路径）" [10: §3.2; 16: §4.2]
  - *orthogonal to* [9] (Blackboard MAS) [med]：[9] 处理任务执行期临时通信（β broadcast + β_r 私有响应，任务范围 ephemeral）；[16] 处理跨任务持久记忆演化（insight 图跨 episode 累积）；时间尺度不同且可叠加——G-Memory insight 图可作为 Blackboard 的跨 episode 背景知识层，与执行期 β 通信正交 [9: §3; 16: §4]
  - *orthogonal to* [6] (MAST) [med, 推断 low]：MAST FM-1.3 步骤重复（15.7%）与 FM-2.5 被忽略输入正好是 [16] insight 层（记录过去重复失败）和 role-specific 注入（防盲目启动）的潜在缓解靶点；但 [16] 未引用 MAST、未量化 FM 级缓解——该关系是推断而非实证 [6: §4; 16: §5.2]
  - *builds-on* [1] (COS-PLAY) [low]：均以"从历史 episode 积累结构化经验"为核心——[1] 积累可复用 skill（双 agent 协同进化），[16] 积累 interaction + insight（MAS 协作轨迹蒸馏）；[16] 将 [1] 的单 agent skill 积累范式扩展到多 agent 协作轨迹维度 [1: §3; 16: §1]
  - *relation to thesis* — 直接命中 thesis **goal 5（跨会话层级化 Memory）** 与 **goal 3（Shared Workspace 协同信息保真度）**：(a) **三层结构 → User/Role/Org 原型对照 [med]**：interaction（User 级执行细节）/ query（Role/task 级目标索引）/ insight（Org 级泛化策略）与 goal 5 三层在抽象层次上同构；insight 层独立贡献 3.95%↓（ablation）为"泛化层独立可测"提供量化佐证 [16: §5.4]；(b) **Role-specific 注入 ↔ Shared Workspace 上下文压力降低 [med]**：算子 Φ 的 role 过滤与 thesis "多 agent 协作时须按角色分发上下文"设计原则直接对应；"通用单 agent 记忆注入 MAS 反而降低性能" 是对该原则的反向工程论据 [16: §5.2]；(c) **自演进 insight vs TraceAI 审计 gap [治理优先]**：insight 由 LLM 自动生成、无溯源性——与 thesis "主动行为 TraceAI-first + action provenance explicit" 直接冲突；落地需补齐 insight 生成 + 写入的 TraceAI 审计条目（内容 / 触发 query / 生成时间 / LLM 参数）；(d) **无衰减/遗忘 → goal 5 工程空缺仍开放**：G-Memory 三图均单调增长，与 [10] 同缺 decay + 冲突合并——确认"治理与遗忘机制"是 goal 5 独有工程盲点，文献中无现成解法；(e) **新增可证伪点**：(e1) "G-Memory 1-hop 最优性在企业级任务异构分布（数据分析/文档审核/工作流审批混合）下仍然成立"——benchmark 同域假设使 1-hop 精准，Decision Agent 跨域任务 query graph 边质量待验证；(e2) "G-Memory LLM 生成 insight 在企业规管内容场景幻觉率 < 5%"——无任何忠实度评测。证据强度：跨框架/backbone 一致增益 [med]；消融独立可测 [med]；无企业场景验证、无 TraceAI 对齐、无衰减机制——**应作为 goal 5 MAS 跨轨迹自演进机制的参考原型，不应作为"MAS 层级记忆在企业多步 AutoFlow 场景下有效"的直接证据** [16: §Relations; thesis goals 3, 5]

> *Emerging sub-cluster: Tool Retrieval Benchmarking & Instruction-Augmented IR* — 跨 30+ 公开 tool-use 数据集统一为 BEIR/MTEB 风格 IR 评测，量化"现成 dense / cross-encoder / LLM agent re-ranking 在工具检索上系统性崩盘"，并提出 instruction-augmented 协议 + 工具检索专用微调（ToolRet-train）。与 §1.5 Catalog-Layer Tool/Agent Retrieval 同处"调用前检索"层，但视角是评测/训练侧而非架构侧。[11] 不适配既有子簇，放置于父桶 §1；建议增设 §1.10，详见 `contradictions.md §Proposed taxonomy extension`。

- **ToolRet** [11] (Shi et al., 2025) — 自 30+ 公开 tool-use 数据集汇总 7.6k 任务 / 43k 工具，按 BEIR/MTEB 风格统一格式 + 三专家 100 条 seed instruction + GPT-4o 自动生成 instruction（89.2% 通过专家审查、Kappa 0.743）；评测 sparse / dense / cross-encoder / LLM agent re-rank 五类 IR 模型在 NDCG@K / Recall@K / Precision@K / Completeness@K（COLT 提出）四指标 + `w/o inst.` vs `w/ inst.` 两协议下的表现 [11: §3, §5]。**主结果**：6.4B 参数级 NV-Embed-v1 平均 nDCG@10 仅 33.83 / Completeness@10 ≤ 35%，加 instruction 后提升至 42.71 / 43.41（+7~17 nDCG 点 across 模型）；最强 cross-encoder bge-reranker-v2-gemma w/ inst. 47.52 / 48.90 仍意味 ≥51% multi-target query 无法在 top-10 全召回 [11: §6.1, Tables 4–5]。**端到端**：ToolBench-G1 上把官方人工标注 toolset 换成 IR 召回结果后 GPT-3.5 pass rate 62.0→50.6（−11.4），ColBERTv2 Recall@10 仅 27.3；ToolRet-train（200k+ 指令式实例 + K=10 负采样）微调后 GPT-3.5 / ToolLlama pass rate 同步抬升 10–20 点 [11: §1, §7, Figure 6]。**MTEB 相关性**：ToolRet ↔ MTEB Retrieval Pearson 0.790 / Spearman 0.441，方向一致但绝对水平低 ~20+ 点——"工具检索是 IR 子任务但绝对水平显著更低"[11: §6.3, Figure 5]。**Re-rank 反直觉**：MonoT5 / mxbai-rerank 在 w/o inst. 下使 NV-Embed-v1 nDCG@10 反而退化（33.83→28.92 / 24.84），bge-reranker-v2-gemma 是少数带正向收益的；ColBERTv2 w/o inst. 19.46 < BM25s 22.32 [11: §6.1]。**关键缺陷**：(a) one-to-many 标签噪声未量化，Completeness 33% 这种极低数字未做"top-3 任意语义等价"灵敏度；(b) 端到端只在 ToolBench (G1/G2/G3) 上跑 pass rate，ToolACE / Gorilla / CRAFT 未上端到端；(c) instruction 由 GPT-4o "知道 target tool"前提下生成，oracle-aware 风险未做盲写 sanity check；(d) 训练数据 (ToolACE/APIGen/ToolBench train) 与测试侧同源，去重审计未公开；(e) ToolBench 单数据集贡献 ~32% 工具，分数可能被拉偏 [11: §Weaknesses]。
  - *builds-on* [5] (Agent-as-a-Graph) [high]：[5] 在 LiveMCPBench 上把 ToolRet 类的 dense 检索器（BGE/E5）与 MCP-Zero 都列为基线并以图结构 + 类型加权 RRF 全面胜出；[11] 把同一谱系（dense / cross-encoder / LLM re-rank）拉到统一 7.6k 任务 / 43k 工具的横向对照——[5] 给"图结构胜出"局部上界，[11] 给"非图基线全集崩盘"下界 [11: §Relations]
  - *competes-with* [7] (MCP-Zero) [high]：同一研究问题（"现成 IR 在工具检索上瓶颈"）下两条路径——MCP-Zero 押注 inference-time 模型主笔结构化请求，[11] 押注 instruction-augmented 评测协议 + ToolRet-train 微调。两者本质同源：[11] instruction 由 GPT-4o 离线 target-aware 生成 vs [7] LLM 在线主笔；二者证据互证"仅 query 直接 dense 检索瓶颈在 ~30 nDCG@10，必须改写或微调" [7: §5.3, 11: §6.2]
  - *competes-with* COLT (Qu et al., 2024) [high]：[11] 显式把 COLT 列为基线，新训练的 ToolRet-train 模型在 Web/Code/Custom 三子集上全面超过 COLT；同时 Completeness@K 指标本身来自 COLT [11: §5, §6]
  - *builds-on* RAG-MCP / ScaleMCP / Tool2Vec / AnyTool / ToolRerank [med]：§2 把这些方法纳入 "retrieval-augmented tool selection" 谱系但实验未复现作 IR 基线——与 [7] 同样的实验空缺 [11: §2 vs §6]
  - *orthogonal to* [3] (FLARE) [med]：FLARE 关注步进推理多步规划的结构性失败，[11] 在"已分解为单步检索"后评测 IR；可叠加（planner→子步生成 instruction→ToolRet 类 IR），[11: §8 Future Work (ii)] 明确希望评估 interleaved retrieval-and-calling
  - *orthogonal to* [6] (MAST) [med]：MAST 区分工具 schema 注入成本（FC1）与协同失真（FC2）；[11] 解决 FC1 的"召回精度"维度，未触及 FC2。但 ToolRet 数据可作为 FM-2.6 推理-动作错位的上游归因证据：当 IR Recall@10 < 50% 时下游 LLM 即使完美决策也找不到工具
  - *orthogonal to* [4] (OneFlow), [2] (GoA), [1] (COS-PLAY), [8] (HSC), [9] (Blackboard MAS), [10] (Collaborative Memory) [low]：层级正交（折叠等价 / 输出协调 / 技能进化 / 时序调度 / 通信结构 / memory 共享）
  - *relation to thesis* — 直接命中"工具幻觉的语义解耦防御 / ContextLoader 5k–10k 工具规模有效性"研究缺口（thesis §Design Context #3）。四点对 Decision Agent 的可操作启示：(a) **现成 dense 检索器不足以支撑 ContextLoader 5k–10k 规模**——NV-Embed-v1 在 43k 工具池 nDCG@10 仅 33.83 / Completeness@10 ≤ 35%，加 instruction 也只 42.71 / 43.41；ToolRet-train 这类"指令式 + 多源 + K=10 负采样"工具检索专用微调是工程必需而非 nice-to-have；(b) **Instruction-augmented 协议 ↔ BKN 解耦 / [7] 主动请求同源**——本文 instruction = "面向 target tool 的功能描述 + 任务约束"，[7] `<tool_assistant>` 块 = "主动权限域 + 操作类型"；两条独立证据线（[11] +7~17 nDCG 跨 30+ 数据集 / [7] +25 pp APIBank Full）共同支持 thesis "仅靠用户原始 query 做工具检索是错误的设计点"；(c) **Completeness@10 ≤ 50% 是 ContextLoader 的设计警钟**——multi-target query 即使最强 reranker 也只 48.90% 全召回；ContextLoader 必须支持 K > 10 + 二段筛选（语义 retrieval → 结构化 BKN 过滤），而非单段 top-10；(d) **MTEB Pearson 0.79 + 绝对值低 ~20 点是可证伪点的具体设计**——若 KWeaver BKN 工具集复测中相关性≈0.79 + 绝对值≈低 20 点的关系不再成立（如 BKN 解耦后绝对值反而提升），则可作为 BKN "语义结构化是工具检索专属归纳偏置"的工程级证据；反之若仍相关同档下降，则需回到训练侧（ToolRet-train 路径）寻找增益。**新增可证伪点（建议加入 thesis 表）**："ContextLoader 仅做 BKN 类型化 + 通用 embedding（无工具检索专用微调）已足以支撑 5k+ 工具规模"——证伪条件：BGE/E5 等通用 embedding 在 BKN 增强后 Recall@10 仍 < 50%；当前状态：[11] 间接支持该证伪条件（通用 embedding 在 43k 工具池 Recall@10 ≤ 52%，且无 BKN 类型化辅助）。证据强度：retrieval 端绝对数字 [high]，instruction 增益 [high]，端到端 pass rate 增益 [med]（仅 ToolBench），ToolRet-train 微调可迁移性 [med]（训练 / 测试同源去重审计未公开）[11: §Relations; thesis Design Context #3]

- **ToolOmni** [13] (Huang et al., 2026, HIT Shenzhen, arXiv 2604.13787) — 端到端 RL 驱动的开放域工具使用框架：主动迭代检索（迭代 `<search>qt</search>` 查询，最多 4 轮）＋接地执行（Thought-Action-Observation 循环，上限 6 轮）级联串联，全局策略 πθ 通过 Decoupled Multi-Objective GRPO 联合训练两阶段（检索 reward Rret = α1·rfmt + α2·rrec·rconv；执行 reward Rexec = β1·rfmt + β2·rans；分步梯度更新避免量级冲突）[13: §3.2, §3.3.3]。**主结果（ToolBench 16k+ API）**：Multi-Domain NDCG@5 78.29%（对比最强基线 IterFeedback 73.16%，+5.13 点）；端到端 SoPR 54.13% / SoWR 50.16%（均第一）；Category Gen. SoPR 55.95%（超 GPT-3.5 +13.85 点）[13: §4.2, §4.3, Table 2]。**消融**：去除 RL 检索 NDCG -28.99%（RL 是检索精度主驱动因）；去除 SFT 执行 SoPR -9.1%（SFT 是执行质量主驱动因）[13: §4.5, Table 4]。**Golden Tool 实验**：向 Agent 直接给出正确工具集后 SoPR 仅 54.7%（vs 端到端 54.1%），揭示执行推理是与检索等量级的独立瓶颈 [13: §4.3, Table 2]。**关键缺陷**：(a) 级联顺序硬约束——执行阶段不可重入检索，动态 API 发现需求无法响应 [13: §Limitations]；(b) 只在 ToolBench 单 benchmark 完整评测，端到端 SoPR 未在 APIBench / Gorilla 验证；(c) backend 仍是 dense embedding（ToolRetriever），继承 [11] 揭示的低 ROUGE-L 词面重叠上限问题；(d) 执行 reward 依赖 GPT-5 Mini judge，训练测试 judge 同源存在偏好共享风险；(e) 仅用 Qwen3-4B，更大模型解耦 GRPO 增益曲线未知 [13: §Weaknesses]。
  - *builds-on* [11] (ToolRet) [high]：[11] 确立 dense 检索器在 ToolBench 上限（NV-Embed-v1 nDCG@10 33.83 / Completeness@10 ≤ 35%）并证明 instruction-augmented 查询 +7–17 nDCG 点；ToolOmni 沿"让模型改写查询"这一 [11] 指向路径，以迭代 RL 把 NDCG@5 推至 78.29%，相当于把 [11] 离线 instruction 增益内化为在线 RL 能力；[11] 的"instruction 训练推理两侧独立有效"与 ToolOmni 的"SFT 驱执行、RL 驱检索"两阶段设计相互映证 [13: §4.5; 11: §7.2]
  - *builds-on* [7] (MCP-Zero) [high]：共享核心前提——模型主动生成语义化查询（而非直接用 user query）能显著提升工具召回；MCP-Zero 无需训练（prompt-only，APIBank Full +25–30 pp）；ToolOmni 通过 GRPO 端到端训练该策略，消融量化 RL 相对 prompt-only 增量（w/o RL 检索 NDCG -28.99%）；两者共同佐证"query 改写是工具检索必要前置" [13: §4.5; 07: §5.3]
  - *extends* [5] (Agent-as-a-Graph) [med]：[5] 在 LiveMCPBench（70 servers / 527 tools）以类型化 KG + RRF 胜出 dense 基线（Recall@5 0.85 vs 0.70）；ToolOmni 在量级更大的 ToolBench（16k 工具）以迭代 RL 查询生成达 78.29% NDCG@5；两者选用不同归纳偏置（类型化结构 vs 查询策略 RL），原则上可叠加；[13] 工具规模更接近企业目标但未报告多步链 Completeness 曲线 [13: §4.2; 05: §4.2]
  - *orthogonal to* [3] (FLARE) [med]：执行阶段是 ReAct-based（6 轮上限），直接继承 FLARE 步进推理深度退化局限；检索阶段多轮 query 规划引入有限前瞻，但执行 budget 仍被硬限——Decision Agent 长链 AutoFlow（>6 步）场景直接制约任务完成率 [13: §4.3; 03: §4]
  - *orthogonal to* [6] (MAST) [low]：单 agent 范畴，不涉及多 agent 协同失真（FC2）；减少工具检索噪声能间接降低 FM-2.6（reasoning-action mismatch），每查询 API 调用从 3.98 降至 2.47 次 [13: §4.7; 06: §3.2]
  - *orthogonal to* [12] (Component Composition) [low]：[12] 把 agent/skill 组合建模为背包优化；ToolOmni Tsub 工具子集选取在结构上与背包约束相似（有限上下文 budget），但 [13] 以 RL rconv reward 隐式鼓励精简、未用资源约束优化框架；两者处理不同粒度（agent 组合 vs 工具子集）但可在 Composer 层面联合设计 [13: §3.3.1; 12]
  - *relation to thesis* — 直接命中"工具幻觉的语义解耦防御 / ContextLoader 5k–10k 工具规模有效性"研究缺口（thesis §设计上下文 #3）。三点对 Decision Agent 的可操作启示：(a) **RL 查询改写比 SFT-only 多 28.99% NDCG——ContextLoader 查询前置改写必须进入训练 pipeline [high]**：BKN 语义解耦是正确方向，但只做离线结构化而不做在线 RL 优化，检索精度上限严重低估；Decision Agent ContextLoader 在 5k–10k 工具规模可用精度需考虑 ToolOmni 式检索 reward（recall × conversion 细粒度设计）[13: §4.5, Table 4]；(b) **Golden Tool 实验揭示执行推理是独立瓶颈——不能把提升工具召回等同于提升任务通过率 [high]**：SoPR 54.7%（完美工具）vs 54.1%（端到端）差距约为零，表明 Composer 规划质量与 Execution Factory 执行健壮性是独立于 BKN/ContextLoader 的第二条瓶颈线；thesis 可证伪点设计需分离检索侧与执行侧两维度 [13: §4.3, Table 2]；(c) **ToolBench 16k API 规模最接近企业目标，但文档标准化（RapidAPI JSON schema）与 KWeaver BKN 工具文档质量不等，结论必须在 BKN 工具集复测 [med]** [13: §4.1; thesis §可证伪点追踪]。**新增可证伪点（建议加入 thesis 表）**："在 BKN 工具集 5k+ 规模，ToolOmni 式检索 reward（rrec × rconv）比静态 instruction-augmented BKN embedding（无 RL 训练）高 ≥10% NDCG@5"——证伪条件：离线 instruction 增益已足够覆盖 RL 增量（本文 SFT-only NDCG 49.3% vs 全量 78.3%，差 ~29 点）。证据强度：检索侧 NDCG 绝对值 [high]（多域 + 多基线对照）；消融信号 [high]（w/o RL -28.99% 清晰量化）；执行侧 SoPR [med]（单 benchmark + LLM judge 自循环）；Golden Tool 瓶颈实验 [med]（方法明确但仅 ToolBench 单 benchmark）[13: §Relations; thesis Design Context #3]

> *Emerging sub-cluster: Knapsack-Based Component Composition with Sandbox Validation* — 把"从已有组件库（工具 / sub-agent）中选最优子集组装智能体"形式化为带预算约束的 knapsack，配合"composer 生成 skill → 检索候选 → 沙箱实测 → ZCL 在线阈值收/弃"design-time pipeline；与 §1.5 Catalog Retrieval（pure 语义检索）和 §1.10 Tool Retrieval Benchmarking（IR 评测 / 训练）形成第三条路径——以**测试**而非更好的 embedding 或更好的 IR 训练来对抗描述-能力错配。[12] 不适配既有子簇，放置于父桶 §1；建议增设 §1.11，详见 `contradictions.md §Proposed taxonomy extension`。

- **Component Composition (Knapsack)** [12] (Yuan et al., 2025, NeurIPS 2025) — 把组件选择形式化为 S* = argmax pτ(S) s.t. Σci ≤ B 的预算约束 knapsack；composer agent 工作流：(1) 把任务描述解析成 ≤6 个 skill（name + description + importance + 测试 query 列表）；(2) 按 skill description top-K=10 语义检索（BGE-Large-English）；(3) 对每个候选组件在 base agent (CodeAct/ReAct) 上跑测试 query，composer 自身做 LLM-as-judge 判断 helpful/broken；(4) ZCL 在线阈值 Ψ(z) = (U / (L·e))^z · L·e（z = 已用预算占比，L = 1/max(c)，U = Σwj/min(c)）按 ratio ρi = vi / ci ≥ Ψ(z) 决定收/弃，理论 ln(U/L)+1 竞争比 [12: §3, §4, Algorithm 1]。**单 agent Pareto 主结果（Claude 3.5 Sonnet）**：GAIA online knapsack+OPT($30) 0.47 success @ $14 vs identity 0.47 @ $398；SimpleQA 0.92 @ $12 vs identity 0.80 @ $398；MedQA 0.93 @ $6 vs identity 0.92 @ $398——对纯 retrieval baseline 最多 +31.6 pp 绝对提升 [12: Abstract, §5.1, Figure 3]。**多 agent 主结果**（117 sub-agent inventory，含 10 个合成 distractor）：Travel online knapsack($6) overall/partial 0.40 / 0.69 vs identity 0.07 / 0.09；Mortgage 0.87 / 0.93 vs identity 0.07 / 0.02 [12: §5.2, Figure 4, Table 5]。**定性证据**：纯 retrieval 在 GAIA / SimpleQA 系统性漏选 web search 工具——skill description "Web Browsing" 与 `get_article_content` 描述语义最近、与 `web_search_paid` 反而较远 [12: §6, Table 2]。**跨 8 backbone 一致性**：online knapsack ≥ offline knapsack ≥ retrieval；SimpleQA + Claude 3.5 Sonnet 三次重复 σ ≤ 0.034 [12: Appendix A.9, Tables 4–12]。**关键缺陷**：(a) 多 agent 实验 sub-agent 价格全统一 $1，让 ZCL 退化为"value 阈值"——0.07→0.87 巨大提升不能干净归因 knapsack 算法 [12: §5.2 Setup]；(b) "distractor agent" 是论文自合成（描述重叠 + 无工具），inventory 从 ~20 拉到 117 是 synthetic 而非真实 inventory，retrieval 失败的外推性弱 [12: Appendix A.6]；(c) 沙箱判别 self-judging：composer 既出 skill 与 test query 又作 judge，无人类 ground truth、无 calibration / 多评判一致性度量 [12: §4, Appendix A.2]；(d) inventory 仅 120 工具 / 117 agent，远低于 ContextLoader 5k–10k 目标，无规模 scan / 无 inventory size × budget 扩展曲线 [12: §6 Limitations]；(e) sandbox 是 design-time 一次性测试，不在生产期重测——schema 漂移 / 模型升级失效率未量化；(f) §3 形式化的"组件间非加性交互"被 Algorithm 1 忽略（vi 假设可加），synergy / conflict fallback 缺失；(g) GAIA 上 online knapsack 0.47 = identity 0.47 而 cost $14 vs $398——但未做随机抽样 4 工具子集 baseline，无法排除"identity 失败的根因是 122 工具撑爆 prompt 而非工具选择本身"，cost 节省核心解释偏向"context bloat 缓解"而非"knapsack 最优"；(h) AvaTaR prompt 优化在多个设置下出现回退（Table 6 GAIA Claude 3.7 Sonnet 0.50→0.44）但只在 Limitations 一笔带过 [12: §Weaknesses]。
  - *builds-on* [11] (ToolRet) [high]：§2 与 §5.1 显式引用 ToolRet 并使用其工具集构造 inventory；Identity & Retrieval 在 GAIA / SimpleQA 上的失败被论文用作 [11] "现成 IR 不够工具感知"主张的进一步证据 [12: §2 Tool Retrieval, §5.1 Inventory]
  - *competes-with* [7] (MCP-Zero) [med]：MCP-Zero 把"克服检索失败"放在 inference-time 由模型主笔结构化请求，[12] 放在 design-time 由 composer 沙箱测试 + ZCL 阈值；都拒绝纯静态语义检索但选择不同层（运行时 vs 装配时）介入；[12] 未引用 [7]
  - *competes-with* [5] (Agent-as-a-Graph) [med]：[5] 用类型化 KG（BKN-同构）graph 结构防描述-能力错配，[12] 用 sandbox 实测防同一类错配；两者解决同一问题（GAIA 上 retrieval 漏选 web_search 类）的两条独立证据线
  - *contradicts* [4] (OneFlow) [med]：OneFlow 主张同质 MAS 可被等价折叠为单 LLM；[12] §5.2 在 117 sub-agent + supervisor 设置下 identity 0.07 vs online knapsack 0.87，反向表明：当 inventory 足够大且任务跨域时，"折叠"等价构造会被 supervisor 的 delegation capacity 绑死，多智能体反而是必要的——但需小心：[12] 是从已有 sub-agent inventory 选子集而非真做 OneFlow 式 prompt 折叠，严格说是"在选择存在的前提下，选择策略至关重要"，未直接复测 OneFlow 折叠假设。与 [9] DS-GRU baseline 形成"OneFlow 折叠等价的两条边界条件"：[9] 上下文必然溢出场景物理无法承载，[12] supervisor delegation capacity 在 100+ 跨域 agent 场景被绑死；详见 `contradictions.md §Contradiction: OneFlow 折叠等价的第二条边界`
  - *orthogonal to* [9] (Blackboard MAS) [med]：[9] helper 在每个请求上 self-nominate（runtime self-discovery），[12] composer 在装配前一次性沙箱（design-time pre-validation）；二者在"如何对抗中央 capability registry 失准"上互补——design-time 给静态 inventory 做能力清洗、runtime 给动态请求做 last-mile 路由
  - *builds-on* [2] (GoA) [low]：均属"多 agent 系统设计自动化"大类（ADAS Hu et al. 2024 框架），但 GoA 优化通信图与节点能力、[12] 优化 agent 选取 + 预算约束；[12] 未引用 GoA
  - *orthogonal to* [6] (MAST) [med]：MAST 14 类失败模式刻画系统已组装后的运行期失败，[12] 刻画组装期"选错组件"失败；distractor agent 被选中后 supervisor delegation 失败实际可被 MAST FC1 (specification) 与 FC3 (verification) 部分覆盖，[12] 未做该映射
  - *orthogonal to* [1] (COS-PLAY) [low]：[1] 让 skill 库随 trace 反馈增长（runtime 学习），[12] 让 composer 在装配时清洗 skill 库（design-time 选择）；方向相反但共享"skill / 组件库不是静态资产"判断
  - *orthogonal to* [3] (FLARE), [8] (HSC), [10] (Collaborative Memory) [low]：层级正交（步内规划深度 / 跨步时序调度 / 跨用户记忆共享）
  - *relation to thesis* — 同时命中 design **goal 1 (开箱即用模板)**、**goal 4 (Composer 轻量多智能体组合)** 与"工具幻觉的语义解耦防御"研究缺口（thesis §Design Context goals 1, 4 + #3）。可操作影响：(a) **Composer 三段式 pipeline 直接同构 [high]**：[12] composer agent 的"生成 skill → 语义检索候选 → 沙箱实测 → 预算约束选择" 与 thesis goal 4 "自然语言描述任务 → 可审查 Agent Team Plan → 调度执行" 在结构上同构——尤其 skill 生成 + sandbox validation 是 thesis 之前未涵盖的具体落地范式，[12] 提供 prompt 模板（Appendix A.1–A.3）可作为 Composer Plan 节点的工程起点；(b) **Sandbox-validated capability registry [med, 治理张力]**：[12] design-time 一次性 sandbox 测试 vs [9] runtime self-nomination 是关于"capability registry 何时校准"的两种解法，前者更契合 thesis"action provenance must be explicit"治理要求（沙箱轨迹可全量进 TraceAI），后者更契合 5k–10k 规模动态调度——Decision Agent 落地 Composer 时应取分轨：BKN 静态登记 + 周期性沙箱 + 运行时 last-mile 路由；(c) **预算约束是 Decision Agent 多租户场景的天然拓扑 [med]**：thesis 反复强调"上下文窗口工程约束"，knapsack 框架把约束从隐式 (token 预算) 提升为显式 (component cost) 决策变量，与 ContextLoader "工具规模 ×10 + 上下文 −90%" 的工程指标可直接对接；但 [12] 在 multi-agent 场景将所有 cost 设 $1（ZCL 退化），cost-heterogeneous 场景未验证，自家 BKN 工具集 cost 跨 free→paid→infra 多档时 ZCL 竞争比 ln(U/L)+1 的实际表现需要复测；(d) **GAIA 持平观察支持 ContextLoader 价值命题 [low]**：identity 122 工具 0.47 @ $398 与 online knapsack 4 工具 0.47 @ $14 的持平更可能由"122 工具撑爆 prompt 上下文"驱动（论文未做控制实验排除），这反向支持 Decision Agent ContextLoader "动态按需加载" 的工程定位——但需作为 [low] 引用，因 [12] 自身未做该归因；(e) **新增可证伪点（建议加入 thesis 表）**：(e1) "Composer 的 design-time sandbox validation 在 5k+ agent / 工具规模下，沙箱总耗时 ≤ N 小时（N 待 Decision Agent 业务可接受度定义）" —— [12] 自报 10–30 min @ 117 agent，5k 规模线性外推 8–22 h，需检查能否分布式并行；(e2) "Knapsack 公式中假设的'组件值可加'在 Decision Agent 多步 BKN AutoFlow 中不引入 >10% 选择错误"——[12] 自承非加性交互被 Algorithm 1 忽略，AutoFlow 链上工具协同 / 冲突若高频则 knapsack 主张失效。证据强度：单 agent Pareto [med]（多 backbone 一致 + 跨数据集，但 sandbox self-judging）；多 agent 0.07→0.87 提升 [low→med]（cost 退化 + distractor 合成构造）；ZCL 理论保证 [high but boundary]（仅 cost-heterogeneous 才发挥 ln(U/L)+1 竞争比）—— **应作为 goal 4 Composer pipeline 形式化原型与 sandbox 范式来源，不应作为"117→5k 规模 design-time 验证可行性"的直接证据** [12: §Relations; thesis Design Context goals 1, 4, #3]

> *Emerging sub-cluster: Importance-Modulated Memory Decay & Forgetting* — 把"记忆 fragment 是否保留"从二元（FIFO/全留）替换为"重要性调制的连续指数衰减 + 双层强度差异 + LLM 仲裁的冲突/融合"，使容量受限下"重要保留、不重要淡出"成为可工程化机制。操作维度是"记忆持久度治理"，与 §1.9（[10] 跨会话访问控制：谁能看到什么）和 §1.12（[16] MAS 跨轨迹自演进：如何从历史学习）形成 goal 5 的第三个独立维度——什么应该被保留多久。[17] 不适配既有子簇（§1.9 关注权限、§1.12 关注层级图），放置于父桶 §1；建议增设 §1.13，详见 `contradictions.md §Proposed taxonomy extension`。

- **FadeMem** [17] (Wei et al., 2026, Alibaba International + 北大软件学院, arXiv 2601.18642) — 双层 LML（半衰期≈11.25 天）/ SML（半衰期≈5.02 天）记忆 + 重要性调制衰减 v_i(t) = v_i(0)·exp(−λ_i·(t−τ_i)^β_i)（λ_i = λ_base·exp(−µ·I_i)、β_LML=0.8 / β_SML=1.2）+ 滞后阈值层迁移（θ_promote=0.7 > θ_demote=0.3）+ LLM 4 类冲突分类（compatible/contradictory/subsumes/subsumed，contradictory 用 v_i ← v_i·exp(−ρ·age_diff) 抑制旧记忆）+ 适应性融合（语义+时间双约束簇 + LLM 验证保留度）[17: §2.1–§2.4]。**主结果**：合成 LTI-Bench 30 天 / 10,780 序列上以 55.0% 存储保留 82.1% 关键事实 / 71.0% 上下文事实，超过所有 100% 存储基线（Mem0 78.4% / MemGPT 75.6% / LangChain 71.2% / Fixed-16K 50.2%）[17: §3.2, Table 1]。LoCoMo 多跳 F1=29.43、FCR=85.9%、SRR=0.45（基线 SRR=0.00–0.15）[17: §3.4]。**消融**：去除 fusion 致 LoCoMo 多跳 F1 从 29.43 降至 13.63（−53.7%）；去除双层 LML/SML 降至 19.45（−33.9%）；去除冲突解决降至 22.88（−22.4%）——三组件均独立可测，贡献顺序 fusion > LML/SML > conflict [17: §3.5, Figure 2]。**关键缺陷**：(a) 评测全部合成或小规模（LTI-Bench 完全自构造、构造方法零披露；MSC 平均 1,614 tokens/会话；LoCoMo 论文级 benchmark；30 天窗口对"长期遗忘"假说支撑不足）[17: §3.1]；(b) ≥13 个旋钮（λ_base/µ/β_LML/β_SML/α/β/γ/θ_promote/θ_demote/θ_sim/θ_fusion/ε_prune/T_max/T_window/ω/ρ/Δv/N），其中重要性权重 α/β/γ 数值未公开，无敏感性分析 [17: §2 vs §3.1]；(c) 写入路径每条新记忆同步触发 LLM 冲突分类，每个高密度簇触发 LLM 融合——延迟与成本未量化 [17: §2.3, §2.4]；(d) "生物启发"是修辞——β_LML=0.8 / β_SML=1.2 等具体数值无神经科学文献支撑，是 30 天窗口的曲线拟合参数；(e) contradiction 类分类 acc 仅 66.2%（33.8% 误分），把 contradictory 错判为 compatible 让旧错误信息保留并污染检索，下游影响未分析 [17: §3.3]；(f) 单 agent 单用户假设——无 user_id / 无访问控制 / 无多租户，跨租户共享空间下"A 用户的高频访问能否影响 B 用户的衰减"未触及 [17: §2 整章]；(g) LLM-as-Judge 同源（GPT-4o-mini 既做 generator 也做 judge），无跨模型 sanity check；(h) SRR 不区分"丢弃"vs"融合压缩"，"45% 存储削减"含两类不可拆分贡献 [17: §3.1 SRR vs §3.4]。
  - *builds-on* [16] (G-Memory) [med]：[16] 的三层图（interaction/query/insight）在我 notes/16 §Weaknesses 列出"无 TTL/decay/pruning/冲突合并"为关键空缺；[17] 的衰减形式 + 冲突分类 + 融合机制正面填补该空缺。两者方向互补：[16] 解决 MAS 跨轨迹"如何从历史学习"，[17] 解决"什么应该保留多久"——叠加是 goal 5 完整治理路径的候选（[16] 提供层级结构、[17] 提供持久度调控），但论文均未互引 [17: §2.3-2.4 vs 16: §4.3]
  - *builds-on* [10] (Collaborative Memory) [med]：[10] 提供 (T, U, A, R) provenance + 时变 bipartite 授权图但**无衰减**（notes/10 §Assumptions 标记"Memory 单调增长，无 decay / forgetting / 过期机制"），[17] 提供衰减但**无访问控制**——两者占据正交维度（"谁能看到什么" vs "什么应该被保留多久"）。Decision Agent goal 5 落地需把 [10] 的 (T, U, A, R) 扩展为 (T, U, A, R, v, λ, β, layer)；衰减状态须按 (fragment, user) 元组独立维护以避免跨租户污染——FadeMem 论文未触及此设计点 [17: §2.1 vs 10: §3.2]
  - *competes-with* [16] (G-Memory) [low]：在"Insight Graph 写入由 LLM 自动生成 + 无 TraceAI 友好性" vs "FadeMem 衰减为闭式确定性可前向计算"上构成两条治理路径——FadeMem 的 v_i(t) 在任何时刻给定 (τ, λ, β) 完全可还原，每次访问/迁移/剪枝可写 TraceAI；[16] insight 生成黑盒，与 thesis "TraceAI-first / action provenance must be explicit" 张力更大 [17: §2.2 vs 16: §4.3]
  - *competes-with* [10] (Collaborative Memory) [low]：在"LLM-based 转写写入策略"上[10] 与 [17] 都依赖 LLM 概率输出且都无失败率评测——[10] 是 redact/anonymize 转写，[17] 是 4 类冲突分类（acc 66.2%）。两者共享同一安全弱点，是 goal 5 借鉴时必须替换为规则引擎 + LLM 兜底的关键设计点 [17: §3.3 vs 10: §4]
  - *orthogonal to* [9] (Blackboard MAS) [low]：[9] 处理任务执行内瞬时通信（task-scoped ephemeral），[17] 处理跨会话持久衰减（cross-session persistent）；时间尺度完全不同。两层接口决策点：blackboard 任务结束时哪些 trace 进 FadeMem、哪些直接丢弃——本论文未涉及 [17: §2 vs 9: §3]
  - *orthogonal to* [6] (MAST) [low]：MAST FM-1.3 步骤重复（15.7%）和 FM-2.6 推理-动作错位（13.2%）部分原因是"agent 记不住或记错先前 trace"；FadeMem contradictory 抑制理论上可缓解 FM-2.6，但 FadeMem 是单 agent 设计，未在 MAS 场景测试 MAST 失败模式——关系是"机制可能相关但未实证"
  - *orthogonal to* [3] (FLARE), [1] (COS-PLAY), [8] (HSC), [11] (ToolRet), [12] (Knapsack), [13] (ToolOmni) [low]：层级正交（步内规划 / 技能进化 / 时序调度 / 工具检索）
  - *relation to thesis* — 直接命中 thesis **goal 5（跨会话层级化 Memory）的"治理与遗忘机制"子目标**——是 corpus 中**首篇**把"主动遗忘"作为头号设计目标且配套形式化公式 + 消融的论文。可操作影响：(a) **填补 v14 报告中标记为"文献空白"的 Q5c/Q5d/Q5e [high]**：FadeMem 同时给出衰减（指数 + 重要性调制）、遗忘（强度阈值剪枝 + 休眠超时）、冲突合并（LLM 4 类分类 + 年龄差抑制）三个机制的形式化与消融——v14 报告的 goal 5 工程问题地图中三个 ✗ 文献空白现在升级为 ✓ 有候选机制（虽然实证质量 [med]）；(b) **衰减机制天然契合 TraceAI [high]**：v_i(t) = v_i(0)·exp(−λ_i·(t−τ_i)^β_i) 是**确定性可前向计算的**——任意 t 给定 (τ_i, λ_i, β_i) 完全可还原，每条 fragment 的 strength 时序变化、重要性更新、层迁移、剪枝事件均可写 TraceAI 审计；这与 [16] G-Memory "LLM 自动生成 insight 黑盒"形成对比——FadeMem 衰减与 thesis 治理优先核心定位结构性兼容；(c) **重要性公式可直接对应 BKN/ContextLoader 信号**：I_i = α·rel + β·f/(1+f) + γ·recency 的三维度可映射为"与当前 task BKN 节点距离 / 该 fragment 在 trace 中引用次数 / 写入时间"——但 α/β/γ 权重未公开，迁移到 BKN 工具集需重做 grid search；(d) **LLM 冲突分类需降级为建议层 [med, 治理张力]**：FadeMem contradictory 误分率 33.8%，企业合规场景下 1/3 错判为 compatible 让错误信息共存沉积——Decision Agent 应取"BKN-anchored 规则 + LLM 兜底"分轨方案：同业务对象同属性冲突直接走 BKN schema 规则确定权威源（newer-wins / authority-tier），LLM 仅在规则不确定时介入；涉及外部副作用的 fragment 写入冲突必须经 Human-in-the-loop（与 thesis contradiction 1 / Option C 改良版的内部 vs 外部副作用分轨一致）；(e) **多租户独立性是 thesis 落地必补的设计点 [high]**：FadeMem 单用户假设让"跨租户共享空间下 A 用户高频访问能否影响 B 用户的衰减"完全空白——Decision Agent 必须明确"衰减状态按 (fragment, user) 元组独立维护"，把 [10] (T, U, A, R) provenance 扩展为 (T, U, A, R, v, λ, β, layer, last_access, prune_reason)；(f) **写入路径同步 LLM 调用与 7×24 性能 [med]**：每条新记忆同步触发 LLM 冲突分类——5k–10k 工具 + 数百并发用户场景下吞吐瓶颈未量化；落地需评估异步化（先入库 + 后台合并）+ BKN 规则前置过滤；(g) **goal 5 完整候选架构形成**：[15] HLTM schema-aligned 树拓扑 + [10] (T, U, A, R) 访问控制 + [16] 跨轨迹 insight 抽取 + [17] 衰减/冲突/融合，四篇论文的合成版本是当前 corpus 给出的最完整 goal 5 落地原型。**新增可证伪点（建议加入 thesis 表）**：(g1) "FadeMem 重要性公式 I_i 在 BKN 工具集上关键事实保留率显著高于纯 recency 基线"——仅在合成 LTI-Bench 验证、α/β/γ 未公开；(g2) "FadeMem 双层 LML/SML 在企业 1 年级时序仍保留关键事实"——30 天窗口对长期遗忘支撑不足；(g3) "FadeMem 写入路径同步 LLM 调用在 5k–10k 工具 / 数百并发用户下可承受"——论文未报告写入 token / 延迟 / 错误率；(g4) "FadeMem contradictory 33.8% 误分率在企业合规场景仍可接受"——若 1/3 冲突错判为 compatible，错误信息沉积，需更严格机制。证据强度：理论形式化 [med]（清晰但缺机制对应论证）；LoCoMo / MSC 公开 benchmark [med]；LTI-Bench / 4075 条冲突注入 [low]（完全合成、构造细节零披露、judge 同源）；超参数迁移性 [low]（≥13 旋钮、α/β/γ 未公开）—— **应作为 goal 5 治理与遗忘机制的形式化原型来源，不应作为"FadeMem 在企业 7×24 多租户场景下安全有效"的直接证据** [17: §Relations; thesis Design Context goal 5]

> *Emerging sub-cluster: Organization-Level Governed Memory with Dual Modality & Schema Lifecycle* — 在 fragment 之上抽象出"组织级治理基础设施"层：组织硬分区 + entity 作用域 (CRM-key) + dual modality (open-set + schema-enforced) + 治理变量分级路由 (fast/full) + progressive context delivery + AI-assisted schema lifecycle。操作维度是"组织级治理 + schema 自演化"，与 §1.9 (跨用户访问控制) 占据正交治理粒度（组织 vs 用户 × agent × resource），与 §1.13 (重要性调制衰减) 在记忆持久度治理上互补（governance vs decay）。[18] 不适配既有子簇，放置于父桶 §1；建议增设 §1.14，详见 `contradictions.md §Proposed taxonomy extension`。

- **Governed Memory** [18] (Taheri, 2026, Personize.ai, arXiv 2603.17787 v1, single-author 工业方报告) — 提出"治理层在记忆原语之上独立"的四层架构：(1) **dual modality 存储**——open-set memory（去共指原子事实，coreference / self-containment / temporal-anchoring 三类启发式 quality gate + 0.92/0.95 余弦阈值去重）+ schema-enforced memory（按组织 schema 类型化 property value with confidence），二者由**单次 LLM 抽取**同时产出 [18: §4.2-4.3]；(2) **分级治理路由**——fast 模式（无 LLM call，~850ms，embedding sim + keyword overlap + always-on boost 加权）/ full 模式（embedding 预过滤 + LLM 4 步结构化分析，~2-5s）/ auto；governance variable 创建时并行 HyPE（合成 query 增强）+ scope inference [18: §5.1-5.2]；(3) **progressive context delivery**——session state 跟踪已交付 governance variable 与 section，后续步只注入 delta；supplementary 项故意不写 session state 留可晋升空间，TTL 24 小时 [18: §5.3]；(4) **schema lifecycle**——AI 辅助创建 → 自然语言交互增强 → trace-grounded rubric 评分 (4 preset + cross-model judge) → 自动 per-property 三阶段细化 (replay → analyze → parallel optimize) [18: §7]。**MemoryEntry 数据模型**：orgId（组织硬分区）+ recordId（实体作用域）+ type + 七维 provenance（contentHash SHA-256 / contentLength / extractionMethod / llmModel / chunkIndex / chunkTotal / redactionApplied / timestamp）+ 两阶段 PII redaction（Pre/Post-Extraction，Presidio 风格正则）[18: §A, §B]。**主要结果（除 LoCoMo 外几乎全部 ≤250 合成样本）**：fact recall 99.6%（4/5 类内容 100% / chats 98%）[18: §8.2]；dual modality 互补 both 34% / open-set only 38% / schema only 12% / both miss 16%（20 样本）[18: §8.4]；治理路由 fast 模式 92% precision / 88% recall（25 variables × 5 类 × 20 任务），well-authored vs poorly-authored variable 发现率差 20-50pp、3/5 类别 poor 版本 0% 发现率 [18: §8.5]；entity 隔离 zero true cross-entity leakage（3,800 results，但完全靠 CRM-key 预过滤而非 embedding distinctiveness）[18: §8.7]；conflict detection 83.3% / full stale suppression 33.3%（30 pairs，半衰期 38 天指数 recency decay）[18: §8.8]；progressive delivery 5 步 50.3% token 节省（再入相同治理域 50-90%，跨入新域 0%）[18: §F E4]；adversarial governance 50 场景 100% compliance + zero policy leakage [18: §8.10]；LoCoMo 整体 74.8%（人类基线 87.9%，−13.1pp；multi-hop 51.7 / temporal 64.6），**open-ended 83.6% 超人类基线 +8.2pp**，但作者自陈与 Mem0/Zep/OpenAI 的 64-67% / 42-66% / ~53% 比较"published systems use varying methodologies, not directly comparable" [18: §8.11]；**E2 记忆密度饱和**：0/3/7/12 → 69.3/86.0/88.0/84.4，作者据此提出"约 7 条 governed memory 即逼近峰值"——但 12 条 84.4 反低于 7 条 88.0 暗示 N=30 噪声拟合 [18: §F E2 vs §10]。**关键缺陷**：(a) 核心数字几乎全部基于 ≤250 合成样本；arXiv v1 单作者 + 单组织自报告生产部署但无任何生产侧 metric（QPS / tail latency / incident rate / 多用户并发吞吐）[18: §8.1-8.2 vs §10]；(b) **multi-agent write conflict 被作者自陈未解决**——§9.1 "concurrent write conflicts under multi-agent conditions remain an open problem"，与论文标题 "Production Architecture for **Multi-Agent Workflows**" 直接冲突 [18: §9.1 vs 标题]；(c) "四层独立可配置"是设计声明而非实证——progressive delivery 依赖 routing session state、reflection 依赖 entity scoping、schema lifecycle 依赖 trace recall flags，无独立性消融 [18: §3 vs §5.3/§6.3/§7.3]；(d) quality gates 是正则与代词模式匹配（作者自陈未与人类标注校准），99.6% recall 实质循环：构造数据时遵循 5 不变量 → gate 检测违反 → 数据天然几乎全过 [18: §4.2 vs §9.1]；(e) entity 隔离 zero leakage 完全靠 CRM-key 预过滤（作者自陈），上游漏标 CRM-key 隔离失效但 E11 不会发现；"embedding distinctiveness"未被实证 [18: §8.7]；(f) 去重阈值 0.92/0.95 跨 embedding 模型可迁移性未评测 [18: §4.2, §D]；(g) HyPE 是 fast mode 关键输入但合成 query 数量/模型/覆盖度全黑盒 [18: §5.1, §E]；(h) E10 reflection ablation 中 manual multi-hop 1 轮 (61.2%) 远高于 API-managed 1 轮 (40.4%) 实质是"研究者人肉拆解 vs API 当前实现能力"对比，无法直接指引调优 [18: §8.6]；(i) "约 7 条饱和"被抬升为产品论断，N=30 + 6 档非单调（12 条 84.4 < 7 条 88.0）的过度泛化 [18: §F E2 vs §10]；(j) schema lifecycle 自动 per-property optimization 仅对内部团队和早期访问开放 [18: §7.2 注脚]；(k) redaction 基于 Presidio 风格正则，作者承认漏 obfuscated/context-dependent patterns；100% adversarial + zero policy leakage 在该 redaction 强度下令人怀疑——50 adversarial 场景未含 PII obfuscation 类（base64 / homoglyph / 多语言混写）[18: §B, §9.1, §8.10]。
  - *competes-with* [10] (Collaborative Memory) [high]：两者都把"多用户多 agent + 双层记忆 + 治理"作为问题但解法路径完全分叉。**[10] 路径**：bipartite 时变授权图 G_UA(t)/G_AR(t) + fragment 级 (T,U,A,R) provenance + policy-level 读写双策略——治理粒度是**用户 × agent × resource 三方矩阵**。**[18] 路径**：orgId 硬分区 + CRM-key 实体作用域 + governance variable 库 + 治理路由 + schema enforcement——治理粒度是**组织 × 实体 × governance variable 三方矩阵**，无用户/agent 维度访问控制。两者覆盖**不重叠的治理需求**，需在 Decision Agent goal 5 落地时**叠加**：(T,U,A,R) 之外再加 (orgId, governance_variable_id, schema_version)；CRM-key 与 agent-level ACL 在哪一级先过滤决定性能与正确性，[18] 未触及 [18: §A vs 10: §3.2]
  - *builds-on* [17] (FadeMem) [med]：FadeMem 提供"重要性 → 衰减 → 强度 → 剪枝"连续动态机制 + LLM-guided 4 类冲突分类（fragment 级），[18] 提供"组织治理 + schema lifecycle"（fragment 之上），二者完全互补且都不解决对方问题——FadeMem 不谈多租户/schema enforcement/governance variable，[18] recency decay 半衰期 38 天是粗粒度时间衰减，无访问频率/语义相关性驱动的主动遗忘。两者叠加正好覆盖 thesis goal 5 "治理 + 遗忘"双向需求；论文均未互引 [18: §6.2 vs 17: §2.2]
  - *competes-with* [16] (G-Memory) [med]：G-Memory 三层是**抽象层级**（trace → query → insight），[18] 四层是**功能分层**（存储/路由/检索/lifecycle）；两者占据 thesis goal 5 不同位置（trace 抽象层 vs fragment 形态层），互补但论文均未互引 [18: §3 vs 16: §4]
  - *builds-on* [15] (HLTM) [med]：两者都把 schema 作为一等公民——HLTM 用 schema 做**结构化检索**（按 facet 切片），[18] 用 schema 做**类型化抽取与下游消费**（property value with confidence）；可叠加为"HLTM 树拓扑 + 节点内 schema-enforced typed property"。[18] schema lifecycle（自动 per-property refinement）显著强于 HLTM "schema 假设外部给定"假设；论文均未互引 [18: §4.3, §7 vs 15: §3]
  - *orthogonal to* [14] (Scaling External Knowledge Input) [low]：[14] 处理静态外部知识库的按需投递，[18] progressive delivery 处理 governance variable 跨步重复注入；同一痛点（"模型在长上下文中间使用率低"）的不同切片，可同时部署 [18: §5.3]
  - *orthogonal to* [11] (ToolRet) [low]：[11] 工具检索 retriever 训练，[18] 不在工具检索域；但 [18] governance routing 92% precision 与 [11] 通用 dense 工具检索弱性形成对比——前者依赖 well-authored variable + HyPE，后者依赖 retriever 训练数据，都体现 retriever 上游"语义信号质量"决定下游路由质量 [18: §5.1, §8.5 vs 11: §6]
  - *extends* [9] (Blackboard MAS) [low]：Blackboard β/β_r 是任务内瞬时通信，[18] governance variable + entity context 是跨任务持久化政策与实体记忆；时间尺度不同（任务级 vs 组织级），可叠加但接口决策点（哪些 trace 写入 governed memory）[18] 未涉及 [18: §4 vs 9: §3]
  - *orthogonal to* [6] (MAST) [low]：MAST FM-2.6 推理-动作错位（13.2%）部分原因是 agent 缺乏一致组织政策视图，[18] governance routing + progressive delivery 可视作针对该 FM 的工程缓解，但 [18] 未在 MAST 失败模式上做评测 [18: §1.1 vs 6: §4]
  - *orthogonal to* [3] (FLARE), [4] (OneFlow) [low]：FLARE 改步内规划深度、OneFlow 同质多 agent 折叠等价；[18] 改跨任务记忆治理且场景为异质 agent 共享同一组织级实体记忆，OneFlow 折叠假设不覆盖
  - *relation to thesis* — 直接命中 thesis **goal 5（跨会话层级化 Memory）的"治理"维度首份产品级架构原型**，并对 thesis 治理优先核心定位、可证伪点追踪表 ≥3 条目产生直接影响。可操作影响：(a) **goal 5 完整工程蓝图浮现 [high]**——[10] (User/Role 维度访问控制) + [18] (Org 维度治理路由 + schema 演化) + [17] (重要性驱动遗忘) + [15]/[16] (层级结构与 insight 抽取) 是当前 corpus 给出的最完整 goal 5 落地蓝图：orgId 硬分区对应 thesis Org 层、CRM-key 实体作用域对应业务对象关联、schema-enforced typed property 是 BKN 在记忆层的镜像、governance variable 路由 + progressive delivery 对应"写入双通道 / 上下文工程治理"概念；但 [18] 未触及 User 与 Role 两层（无用户级 ACL / 无角色继承结构）——与 [10] 互补；(b) **与 thesis 治理优先核心定位高度同构 [high]**——governance variable + schema lifecycle ≈ BKN 业务护栏 + 自演化、provenance 七维元组 ≈ TraceAI 审计字段、redaction 两阶段 ≈ ISF 安全边界、progressive context delivery ≈ ContextLoader 按需加载；但 [18] 治理粒度是组织级 governance variable（"政策"作为字符串/section），thesis 粒度是业务对象级 BKN schema（结构化语义网络）——thesis 粒度更深、表达更强，[18] 是 thesis 治理愿景的简化/弱化版工程实证；(c) **multi-agent write conflict 是 thesis goal 3+5 合并区无法回避的工程题 [high]**——[18] §9.1 自陈 concurrent write conflicts 未解决；FadeMem [17] 单 agent / Collaborative Memory [10] 按用户分区 / [18] 按组织分区，**没有任何已读论文系统性解决"多 agent 同时写同一 fragment"**；Decision Agent 候选解法是 contradiction 1 (Option C 改良版) 的延伸：内部信息处理 fragment 允许 last-writer-wins，外部副作用 fragment 必须串行化 + Human-in-the-loop；(d) **governance routing 作为 ContextLoader 延伸视角 [med]**——thesis ContextLoader 聚焦"工具/BKN 节点按需加载"，[18] 把同一思想延伸到"组织政策/合规规则/模板/指南"维度——ContextLoader 应**扩展为统一的"治理-工具-业务对象"三类候选共享一套召回基础设施**：fast 模式 (~850ms) 高频低延迟 / full 模式 (~2-5s) 低频高复杂度；(e) **progressive context delivery 与 thesis"上下文窗口工程约束"完全对齐 [high]**——5 步 50.3% token 节省（再入相同治理域 50-90%）为 Decision Agent 在 Heartbeat + Cron + Composer 多步路径上的 token 累计风险提供可直接复用策略。**新增可证伪点（建议加入 thesis 表）**：(f1) "约 7 条 governed memory 饱和"在 BKN 工具集 + 企业实体记忆下是否仍是工作点（[18] N=30 + 12 条 84.4 < 7 条 88.0 暗示噪声拟合）；(f2) governance routing 92% precision / 88% recall 在 5k+ governance variable + 100+ 任务类型规模下是否仍成立（[18] 25 vs 20 远低于企业级）；(f3) schema-enforced 的 38% 长尾事实空白能否被 BKN schema 自身吸收（即 BKN 是否需保留 open-set 模态）；(f4) multi-agent concurrent write conflict 能否用"内部 vs 外部副作用分轨 + Human-in-the-loop"处理（contradiction 1 决议的延伸验证）。证据强度整体 [med-low]——单作者 + 单组织自报告 + ≤250 合成样本 + 与同类比较口径作者自承不可比 + 关键 schema lifecycle 仅对内部团队开放 + multi-agent write 作者自陈未解决。**应作为 goal 5 "组织级治理 + schema lifecycle"参考架构与工程模板（provenance 字段集 / 两阶段 redaction / fast-full 双模式路由 / progressive delivery session state / 自动 per-property refinement），核心数字不应作为基线**——落地前需在自家工具/BKN/企业实体数据上重测 [18: §Relations; thesis Design Context goal 5]

> *Emerging sub-cluster: Pre-Action Action-Level Governance Deliberation* — 把治理从"agent 推理之外的外部 filter"重定位为"内化进 agent 推理本身的 deliberation"：在每次 consequential action 之前强制经过 4 阶段 PAGRL（intent → rule retrieval → permissibility reasoning → decision），consult 一个分 4 层（Global / Workflow / Agent / Situational）的级联规则集，并产生结构化 reasoning trace。操作维度是"动作层治理"，与 §1.14 (Governed Memory) 形成同作者群同期产出的"动作 vs 记忆"治理配对，与 §1.6 (MAST 失败诊断) 在治理目标上互补——前者作设计预防，后者作事后归因。[19] 不适配既有子簇，放置于父桶 §1；建议增设 §1.15，详见 `contradictions.md §Proposed taxonomy extension`。

- **PAGRL** [19] (Bandara et al., 2026, Old Dominion 等多家联合，arXiv 2604.25684 v1) — 提出 **Pre-Action Governance Reasoning Loop**：在每次 consequential action 前强制 4 阶段——意图形成 → 规则检索 → 可允许性推理 → 合规决策（PROCEED / SELF-CORRECT / ESCALATE），每次执行无论结果都生成结构化 reasoning trace [19: §4.1]。**4 层级联治理**：Global（普适不可覆盖）/ Workflow（域级）/ Agent（角色级）/ Situational（条件触发临时规则），高层覆盖低层，所有层必须**同时满足** [19: §4.2]。**3 条规则设计原则**：rationale-bearing（附带 why） / positive framing / specificity-generality 平衡 [19: §4.3]。**3 条 escalation 触发**：动作被类别性禁止 / 动作不可逆 / 推理产生真实模糊 [19: §4.4]。**实现**：MCP governance server（rule store + append-only trace log，与 [18] 同一基础设施模板）+ governance prompt constructor（在 LLM 调用前查询并注入适用规则到 system prompt）+ PAGRL enforcement block（system prompt 固定模板）[19: §5.1]。**Flowr 零售供应链案例研究（4 agent × 4 场景 × 10 重复）**：总体合规决策准确率 **38/40 = 95%**、escalation precision 19/19 = 100%、reasoning trace 完整率 100%、平均延迟 0.65s/动作 [19: §5.3, Table 3]。**关键缺陷**：(a) N=40 极小样本，95% 的 Wilson 区间约 [83.5%, 99.4%]，无统计置信报告；(b) 同一 LLM 既做动作发起者又做合规裁判，无独立 verifier，"explainability"被等同于"auditability"——95% "正确"是作者读 trace 主观判断，无第三方标注 [19: §4.1, §5.3]；(c) 关键差异化主张"normative generalization 优于 AgentSpec 类外部强制"仅 N=1 的 Scenario 3 支撑，无对抗 benchmark（HarmBench）复测 [19: §6.2]；(d) **未与任何现有 runtime 治理系统在同一 workflow 上对比**——AgentSpec / Policy-as-Prompt / OPA 仅做功能勾选表，未跑性能基线 [19: §7.1, Table 4]；(e) 7 条规则全量注入路径在企业 50–100+ 规则规模下的延迟与 token 曲线完全未给 [19: §5.1 vs §1]；(f) "consequential action"边界由 agent 自判，agent 把"提交 $45K 采购单"拆成多个被自归类为"非 consequential"的子步即可绕过治理，论文未做该对抗场景；(g) self-correct 振荡 / 多层规则语义死锁 / situational signal 上游可被对抗篡改——三类风险作者承认或暗示但未压力测试；(h) 规则设计三原则全部为声明性，rationale-bearing / positive framing / specificity 三个原则零 ablation [19: §4.3]；(i) "95%"含 S1（只读检索 10/10）+ S4（硬规则触发 10/10）两个非 deliberative 场景——剥离后实质 deliberative 场景 S2+S3 = **18/20 = 90%**，"95%"的产品叙事稀释了 PAGRL 真正要证明的内容 [19: §5.3]；(j) 作者群与 Flowr case study [43] 完全重叠，与 [18] 同期发布形成同团队配套引用闭环；(k) 缺 prompt injection 系统性套件，仅 1 类对抗变体；(l) §6.3 自陈"应补充 deterministic enforcement layer"——若 AgentSpec 类外部强制兜底已存在，PAGRL 的"内化优势"退化为"额外的可解释检查层"，叠加架构下边际价值未讨论 [19: §6.3 vs §7.1]。
  - *builds-on* [18] (Governed Memory) [high]：同作者群（Bandara / Gore / Shetty / Mukkamala / Hass / Rahman / Liang 等核心成员重合）同期产出的**配套**架构论文。覆盖维度互补——[18] 治理"agent 应该看到什么记忆 + 什么组织政策"（**记忆层治理**），[19] 治理"agent 在做出动作前应当如何 deliberate"（**动作层治理**）；基础设施选择一致——MCP server + append-only trace + provenance 字段 + prompt-side 注入 + 4 层架构思路；关键风险共享——两篇都把"production-grade"作为信用基础但都用作者团队自家系统作为案例（Personize.ai / Flowr）、都缺第三方验证、都自陈"multi-agent concurrent write" 或 "adversarial robustness" 是 future work [19: §5.1, §6.4 vs 18: §1, §3]
  - *competes-with* [6] (MAST) [med]：MAST 路径——把治理失败当作"经验现象"，1,642 trace 实证编码 14 类失败 + 类型化工程缓解；PAGRL 路径——把治理失败当作"deliberation 缺失"，4 阶段强制循环。可证伪交集：MAST FM-1.4 信息隐瞒（13.7%）/ FM-2.6 推理-动作错位（13.2%）/ FM-3.3 错误验证（12.4%）正面对应"agent 在动作前未充分 deliberate"——PAGRL 概念上可缓解，但 [19] 从未在 MAST 任何失败模式 / 任何开源 MAS（ChatDev / MetaGPT / AppWorld）上做评测，"95% 合规"无法证明 PAGRL 减少 MAST 任何一类失败 [19: §5.3 vs 06: §4]
  - *contradicts* [8] (HSC) [high]：本论文与 [8] 直接构成 thesis Goal 2 治理张力的两端。[8] HSC 路径——心跳触发"主动认知活动"，Dream Mode 在空闲时进行**自主目标设定（intrinsic goal generation）**；[19] PAGRL 路径——每次 consequential action 前必经 deliberation，三条 ESCALATE 触发条件之一是"action 越权于 agent 治理边界"。直接冲突：HSC 的 Dream Mode 自主目标设定在 PAGRL 框架下应当被识别为"Layer 1 Global 规则禁止的 intrinsic goal generation"。thesis 决议（已记录）：goal 2 必须是 governance-aware bounded autonomy；本论文为该决议提供工程模板——把 HSC 类能力**限定在"建议生成"层而非"自主执行"层**等价于"任何 Dream-derived action 都必须经过 PAGRL ESCALATE 到人审" [19: §4.1, §4.4 vs 08: §IV-C, §IV-B]
  - *competes-with* [9] (Blackboard MAS) [med]：[9] 决定**信息流向**（β_r 私有 vs β 共享），[19] 决定**动作允许性**（PROCEED / SELF-CORRECT / ESCALATE）；二者互补叠加。thesis contradiction 1 / Option C 改良版"内部信息处理走 β_r 私有 + TraceAI 镜像；外部副作用必须直接进 β + Human-in-the-loop"与 PAGRL ESCALATE 三条触发（不可逆 → 必经人审）天然对齐——可作为 Shared Workspace 治理分轨的**正式判据** [19: §4.4 vs 09: §3.2]
  - *builds-on* AgentSpec / Policy-as-Prompt / OPA-based runtime governance（论文外部，§7.1 引用 [12-14]）[med]：本论文显式把 AgentSpec 列为最近技术对手——AgentSpec 用 DSL 定义运行时规则，达到 90%+ 不安全代码拦截 / 毫秒级延迟。[19] 承认 AgentSpec 在保护强度上更高，但批评其"在 agent 推理之外起作用、规则用 DSL 而非自然语言、无层级架构、无神经认知基础"。两条路径角色分工：PAGRL 处理**柔性 / 上下文敏感 / 可解释**治理（rationale-based 推理 / normative generalization / 人类可读 trace），AgentSpec 类处理**硬性 / 确定性 / 不可绕过**治理。论文 §6.3 自陈"应补充 deterministic enforcement layer"——叠加架构下 PAGRL 边际价值需重新评估 [19: §7.1 vs §6.3]
  - *orthogonal to* [11] (ToolRet) [low]：governance prompt constructor 的"按 4 层检索适用规则"在结构上与 retrieval 同构——若规则库膨胀，[19] 的"全量注入"路径会面对与 [11] 相同的 retriever 质量问题。[19] 未触及该规模问题
  - *orthogonal to* [12] (Knapsack Composer) [low]：Composer（thesis goal 4）落地时，PAGRL 应在 Composer 选定的每个 agent 上独立运行——即 Composer plan 的每个动作都经过 PAGRL deliberation——本论文未讨论该叠加
  - *orthogonal to* [14] (Scaling External Knowledge Input) [low]：[14] 处理外部知识投递上下文压力，[19] 处理治理规则注入；两者面对**同一容量约束**（context window）但目标不同。governance prompt constructor 的全量规则注入在企业规模下会与 [14] 治理的外部知识竞争 token 预算
  - *orthogonal to* [3] (FLARE), [4] (OneFlow) [low]：FLARE 改单步规划深度，PAGRL 改动作前合规推理；OneFlow 论证多 agent 折叠等价单 LLM，[19] 论证治理需嵌入 LLM 推理；操作维度正交，可叠加（折叠后的单 LLM 仍需 PAGRL）
  - *relation to thesis* — 直接命中 thesis **治理优先 / Harness-first 核心定位**与 **goal 2 治理张力**两条主线，影响 5 个 design goal 中的 3 个（goal 2 / goal 3 / goal 5）以及可证伪点追踪表 ≥4 条目。可操作影响：(a) **首份"动作层治理"参考架构 [high]**——PAGRL + 4 层规则 + reasoning trace 在动作层与 thesis ISF + TraceAI 几乎一一对应：4 层级联 ≈ ISF 层级化策略骨架；4 阶段 PAGRL ≈ ISF 在每次动作前的强制检查点（但是"在 LLM 推理内"实现，比传统 ISF 外部 hook 更具 explainability 但缺确定性保证）；reasoning trace ≈ TraceAI 审计字段集（值得直接采纳为 trace schema）；3 条 escalation 触发 ≈ Decision Agent ISF + Human-in-the-loop 触发判据。但本论文采用"内化 deliberation + 自然语言规则"路径与 Decision Agent ISF 倾向"外部确定性强制"路径不完全对齐——thesis 应采纳 [19] **架构骨架**（4 层 / 4 阶段 / trace schema / escalation 判据），实现方式应是 ISF 类外部强制 + PAGRL 类 LLM 推理两层并存（[19] §6.3 自身建议）；(b) **goal 2 治理张力工程级解决模板 [high]**——thesis 已决议（由 [8] 触发）goal 2 必须是 governance-aware bounded autonomy，PAGRL 直接为该决议提供工程模板：显式调度策略来源 ≈ PAGRL Stage 1 Intent formation 必须可追溯到具体调度入口；TraceAI-first ≈ Stage 4 强制 trace 在动作执行**之前**写入；ISF + HITL ≈ ESCALATE 不可逆触发；(c) **goal 3 Shared Workspace 形式化判据 [med]**——thesis contradiction 1 决议与 §4.4 escalation 三条触发直接吻合，可把 PAGRL 的 escalation 三条作为β/β_r 路由的形式化判据；(d) **治理 overhead 工作点参考 [med]**——0.65s/动作 + 全量规则注入路径在 50+ 规则规模下需 retrieval-based 注入；与 [18] progressive context delivery 协同（已下发规则不重复注入）；(e) **内化 vs 外部分工 [med]**——Decision Agent ISF 设计应同时实现两层：PAGRL 在前做 deliberation 与 trace，AgentSpec 类（或 OPA）在后做兜底强制。**新增可证伪点（建议加入 thesis 表）**：(f1) "PAGRL ESCALATE 三条触发能否在 Decision Agent 真实业务长链任务中覆盖 ≥90% 应升级动作（漏报率 ≤10%）"；(f2) "PAGRL 全量规则注入在 50+ 规则规模下延迟与 token 开销是否仍 < 1s/动作 + < 20% context budget"；(f3) "PAGRL 内化 normative generalization 优势能否在 BKN 规则集 + 真实对抗 benchmark（HarmBench）上重现"；(f4) "[19] PAGRL + [18] Governed Memory + [10] Collaborative Memory 三层叠加能否在统一 ISF/TraceAI 实现中保持架构一致——若三套 trace schema / 三套 governance variable 不能融合到统一 BKN 表达，则'治理统一基础设施'论点需重新评估"。证据强度：整体 [low-med]——arXiv v1、N=40 案例（实质 deliberative 仅 S2+S3=20）、ground truth 自定义无第三方标注、与 AgentSpec 类基线无对比、对抗鲁棒性仅 1 类变体、author group 与 case study [43] 重叠、与同期 [18] 形成自家配套引用闭环。**作为 thesis ISF + TraceAI 设计的架构骨架与设计语言已足够（4 层规则 / 4 阶段 PAGRL / 三条 escalation 触发 / reasoning trace schema），可直接采纳；作为机制有效性的证据则证据强度低，落地前必须在 Decision Agent 真实 BKN 规则集 + MAST 失败模式 + 对抗 benchmark 上重测，并必须以 AgentSpec 类外部强制层兜底，不能单点依赖 PAGRL 的 95% 合规叙事** [19: §Relations; thesis 治理优先核心定位 + goal 2 决议]
